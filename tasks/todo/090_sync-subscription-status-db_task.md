📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Критический баг синхронизации: Пользователи успешно проходят проверку подписки в боте (нажимают "Я подписалась", бот подтверждает), но в базе данных (таблица `profiles`) их статус `is_subscribed` остается `FALSE`. Из-за этого frontend-gate (Mini App) блокирует им доступ.
</context>

<task>
1. **Найти обработчик подписки:** Открыть файл бота (например, `lib/telegram-bot.ts` или эквивалент), где находится логика обработки нажатия на кнопку "Я подписалась" (обычно это `bot.action` или `bot.on('callback_query')`).
2. **Добавить обновление БД:** Сразу после того, как бот успешно проверил подписку через `getChatMember`, добавить вызов к Supabase с использованием `SUPABASE_SERVICE_ROLE_KEY` (чтобы обойти RLS).
3. **Логика запроса:** Сделать `UPDATE` таблицы `profiles`: 
   Установить `is_subscribed = TRUE` (и `subscribed_at = NOW()`, если эта колонка была добавлена ранее) для текущего `tg_id`.
   ```typescript
   // Пример логики
   await supabaseAdmin.from('profiles').update({ is_subscribed: true }).eq('tg_id', ctx.from.id)