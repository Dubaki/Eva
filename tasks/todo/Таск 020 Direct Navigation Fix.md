📋 Прочитай docs/BOOT.md

<context>
На Vercel кнопка на главной странице не реагирует на клик в некоторых браузерах. Обработчик `onClick` не срабатывает.
</context>

<task>
1. В файле `app/page.tsx` изменить компонент кнопки.
2. Вместо сложной функции `handleStart` с useCallback, обернуть кнопку в стандартный компонент Next.js `<Link href="/test" prefetch={true}>`.
3. Это гарантирует, что навигация сработает на уровне браузера, даже если JS-скрипты еще грузятся.
4. Добавить к самой кнопке атрибут `type="button"`.
5. Сохранить логику записи в БД (если она была), перенеся её в `useEffect` уже на странице `/test` (чтобы не тормозить вход в тест).
6. Запушить в GitHub: `git commit -am "fix: use Link component for instant navigation" && git push`.
</task>