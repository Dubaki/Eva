<context>
Фронтенд успешно читает статус из БД, но при нажатии "Я подписалась" бот проверяет статус без обновления `is_subscribed = TRUE` в БД. Также нижняя кнопка "App" (Menu Button) выдает ошибку "Токен недействителен", так как всё еще требует JWT в URL, от которого мы отказались.
</context>

<task>
1. В файле `lib/telegram-bot.ts` обновить обработчик кнопки "Я подписалась": жестко записать статус в БД, используя `createClient` с `SUPABASE_SERVICE_ROLE_KEY` (установить `is_subscribed: true` по `tg_id`).
2. В файле `app/page.tsx` (или там, где находится Gate-заглушка) полностью удалить проверку токена (JWT) и экран "Токен недействителен". Приложение должно доверять только `tg_id` из `window.Telegram.WebApp.initDataUnsafe.user.id`.
3. Убедиться, что WebApp URL для нижней кнопки (Menu Button) ведет на корень приложения без дополнительных параметров `?token=`.
4. В конце задачи перенести этот файл task из папки tasks/todo в папку tasks/done.
</task>

<rules>
- Не трогать логику 60-дневного таймера на фронтенде.
- Сохранить текущий стиль уведомлений и логов.
- Обеспечить полную совместимость с новой логикой чтения профиля через Service Role Key, внедренной в задаче 094.
</rules>