<context>
Система авторизации успешно отлажена и работает стабильно. Пришло время убрать отладочный код с фронтенда и создать премиальную Админ-панель (Phase 8). Админка должна иметь дорогой, легкий дизайн (Glassmorphism, плавные анимации) и поддерживать рассылку (Broadcast) сообщений с прикрепленными фотографиями через Telegram Bot API.
</context>

<task>
1. **Очистка отладки (`app/page.tsx` или компонент заглушки):**
   Найти и ПОЛНОСТЬЮ УДАЛИТЬ вывод переменных `DEBUG: ID=...`, `RAW DATA: ...`, `DB ERROR: ...`. Оставить только чистый, красивый UI с текстом "Доступ закрыт. Пожалуйста, подпишитесь на канал...".

2. **Премиальный UI/UX Админки (`app/admin/page.tsx`):**
   Реализовать "дорогой" и легкий дизайн (Light Theme).
   - Использовать Tailwind CSS: скругленные углы (`rounded-2xl`, `rounded-3xl`), мягкие тени (`shadow-sm`, `shadow-md`), полупрозрачные фоны (backdrop-blur, glassmorphism), много "воздуха" (padding/margin).
   - Добавить плавные переходы (`transition-all duration-300`) при наведении на кнопки и карточки статистики.
   - Использовать элегантную типографику (серые оттенки для второстепенного текста, жирный черный/темно-серый для заголовков).

3. **Логика рассылки с фото (Фронтенд - Админка):**
   В компоненте Broadcast добавить поле для загрузки изображения (`<input type="file" accept="image/*" />`). Форма должна отправлять данные на API через `FormData`, чтобы передать и текст, и файл.

4. **Логика рассылки с фото (Бэкенд - `app/api/admin/broadcast/route.ts`):**
   Реализовать обработку `FormData`. 
   - Получить список всех пользователей из БД (`supabaseAdmin`).
   - Итерироваться по списку. Если к рассылке прикреплено фото, использовать метод Telegram API `sendPhoto` (передав текст в `caption`). Если фото нет — использовать `sendMessage`.
   - ОБЯЗАТЕЛЬНО: обернуть отправку каждому юзеру в `try...catch`, чтобы блокировка бота одним пользователем не останавливала весь цикл рассылки.

5. В конце задачи перенести этот файл task из папки tasks/todo в папку tasks/done.
</task>

<rules>
- Не трогать логику проверки подписки и JWT (ее больше нет, работает Telegram WebApp Auth).
- Дизайн должен выглядеть профессионально без использования тяжелых внешних библиотек (достаточно Tailwind + Lucide React для иконок, можно добавить framer-motion для появления блоков, если он уже установлен).
- При отправке рассылки выводить Toast-уведомление администратору о начале рассылки и о ее успешном завершении.
</rules>