📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Необходимо внедрить жесткую блокировку Mini App. Даже если пользователь нажал кнопку "App" в меню Telegram, приложение не должно показывать стартовый экран, если в базе данных (таблица `profiles`) поле `is_subscribed` имеет значение `FALSE`. Пользователь должен видеть экран с требованием подписаться.
</context>

<task>
1. **API Check (Backend):** В роуте `/api/user/status/route.ts` убедиться, что возвращается актуальный статус `is_subscribed` из таблицы `profiles` для переданного `tg_id`.
2. **Frontend Gate (Next.js):** В главном компоненте стартовой страницы (`app/page.tsx`):
   - В блоке `useEffect`, где происходит первичная загрузка данных пользователя, добавить проверку флага `isSubscribed`.
   - Если `isSubscribed === false`, состояние приложения должно переключаться в режим "Access Denied".
3. **UI Logic:** Убедиться, что компонент `AccessDenied` (или аналогичный экран блокировки) корректно отображается вместо контента страницы и содержит инструкции: "Пожалуйста, откройте бота и подпишитесь на канал".
4. **Оптимизация:** Проверка должна срабатывать максимально быстро при загрузке, чтобы пользователь не видел стартовый экран даже на долю секунды.
5. Перенести этот файл в tasks/done/ и предоставить лог.
</task>

<rules>
- СТРОГО: Использовать только данные из БД (`is_subscribed`), так как мы уже настроили их обновление в боте.
- НЕ изменять логику таймера 60 дней (они должны работать параллельно: сначала проверка подписки, потом проверка таймера).
- Исполнитель: Claude Code.
</rules>