Интеграция реферальной механики на экран Cooldown (UI + Logic)
File to edit: app/cooldown/page.tsx
Status: UI/UX ENHANCEMENT & FEATURE PORTING 🎨⚙️

🎭 Роль и Контекст
Твоя роль: Senior Frontend Engineer (Next.js, React, Tailwind CSS).
Контекст: Нам необходимо добавить кнопку «Поделиться ссылкой с другом» на экран кулдауна (app/cooldown/page.tsx). Логика работы кнопки должна быть полностью скопирована с экрана app/referral/page.tsx.
Поскольку дизайн экрана кулдауна плотный, тебе нужно аккуратно (ювелирно) освободить место для новой кнопки за счет легкого уменьшения верхних элементов верстки, чтобы не допустить появления вертикального скролла на мобильных устройствах.

🎯 Задачи (Что нужно написать)
Шаг 1: Интеграция логики (State & Handlers)
Добавь в компонент CooldownPage следующую логику (используй паттерны из referral/page.tsx):

Получение ID: Достань tgId из Telegram.WebApp.initDataUnsafe?.user?.id (обязательно с проверкой на клиентскую среду, так как это Next.js SSR). Обеспечь фоллбэк-значение, если tg_id недоступен.

Стейт загрузки: Добавь const [isSharing, setIsSharing] = useState(false).

Функция handleShare: Напиши асинхронную функцию:

Установи setIsSharing(true).

Сделай POST-запрос на /api/share с телом JSON.stringify({ tgId }).

Обработай ответ в try...catch. В блоке finally установи setIsSharing(false).

При успешном запросе (или если ссылка уже отправлена) закрой приложение: if (typeof window !== 'undefined' && window.Telegram?.WebApp) { window.Telegram.WebApp.close(); }.

Шаг 2: Безопасный рефакторинг UI (Освобождение места)
Ничего не удаляй, только модифицируй Tailwind-классы существующих элементов:

Иконка и круги (Песочные часы): Найди верхний motion.div, содержащий hourglass_empty и пульсирующие круги. Уменьши эту композицию на 30% и подтяни вверх.

Решение: Добавь классы scale-70 origin-top -translate-y-2 (или аналогичные отрицательные маржины, чтобы компенсировать отступы).

Текст кулдауна: Найди текст "Тест будет доступен снова через..." внутри карточки.

Решение: Сделай шрифт меньше (text-sm или text-xs) и, если нужно, добавь whitespace-nowrap или сократи внутренние паддинги карточки (py-2 вместо py-4), чтобы текст гарантированно влез в одну строку на узких экранах.

Шаг 3: Добавление новой кнопки
Вставь новую кнопку между карточкой таймера и кнопкой «Связаться с автором» (в самом низу).

Стили: Сделай её визуально главной (Primary Button) — используй те же цвета, скругления и градиенты, что и у кнопки на странице referral/page.tsx.

Текст: "Поделиться ссылкой с другом".

Атрибуты: Добавь onClick={handleShare} и disabled={isSharing}. Если isSharing === true, показывай текст "Отправка..." или лоадер (spinner) внутри кнопки.

🛑 СТРОГИЕ ПРАВИЛА И ОГРАНИЧЕНИЯ (CRITICAL CONSTRAINTS)
НЕ СЛОМАЙ ТАЙМЕР: Логика расчета времени, прогресс-бар и стейт таймера должны остаться абсолютно нетронутыми.

БЕЗ СКРОЛЛА: Вся композиция должна визуально помещаться на одном экране (viewport) iPhone SE. Вертикальный скролл недопустим. Если не хватает места, агрессивнее уменьшай отступы (gap, mb, mt) между блоками.

SSR SAFETY: Любые обращения к window или window.Telegram должны быть защищены проверками (typeof window !== 'undefined'), чтобы страница не падала при сборке/рендере на сервере.

НЕ ТРОГАЙ СТАРУЮ КНОПКУ: Кнопка «Связаться с автором» (и функция openAuthorContact) должна остаться внизу на своем месте и продолжать работать.

Выдай обновленный код файла app/cooldown/page.tsx. Обязательно добавь комментарии // ADDED или // MODIFIED к строкам, которые ты изменил, чтобы ревьюер мог легко их отследить.