📋 ПРОЧИТАЙ docs/BOOT.md. ПРОЕКТ ОСТАНОВЛЕН ИЗ-ЗА КРИТИЧЕСКОГО БАГА.

<context>
Полная блокировка:
1. Ручное изменение `is_subscribed` на `TRUE` в Supabase НЕ открывает Mini App.
2. Бот НЕ меняет значение в базе.
Это означает, что фронтенд и бот либо используют неверные ключи, либо обращаются к неверным колонкам/таблицам.
</context>

<task>
1. **Frontend (Мгновенная диагностика):**
   - В `app/page.tsx` (или где стоит Gate), временно вывести на экран (под надписью "Доступ закрыт") текст: 
     `DEBUG: ID=${tgId}, Subscribed=${userStatus.is_subscribed}`.
   - Мы должны увидеть, какой ID видит фронтенд и какой статус он получает от API.

2. **API (Проверка связи):**
   - В `/api/user/status/route.ts` ДОБАВИТЬ `console.log("API REQUEST:", tgId)`.
   - Проверить SQL-запрос. Убедиться, что это `.from('profiles').select('*').eq('tg_id', tgId)`.
   - ПРОВЕРИТЬ: не перепутаны ли `tg_id` (номер) и `id` (UUID). В базе мы ищем по `tg_id`!

3. **Bot (Запись):**
   - Почему `update` не проходит? ПРОВЕРИТЬ `NEXT_PUBLIC_SUPABASE_URL` и `SUPABASE_SERVICE_ROLE_KEY`. 
   - Убедиться, что бот работает с ТЕМ ЖЕ проектом, что и фронтенд.

4. **Кэш:** Добавить `export const dynamic = 'force-dynamic'` во все роуты API.
</task>

<rules>
- СТРОГО: Показывать отладочный ID на экране "Доступ закрыт", чтобы Заказчик мог сверить его с базой.
- Исполнитель: Claude Code.
</rules>