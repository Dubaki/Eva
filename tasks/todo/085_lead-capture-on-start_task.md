📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Фаза 7 (Сохранение результатов) успешно завершена. Теперь мы внедряем "Захват лида": профиль пользователя должен создаваться в БД в момент первого касания (команда /start в боте), чтобы мы могли отслеживать конверсию "Зашел -> Прошел тест" и делать рассылки тем, кто отвалился на этапе тестирования.
</context>

<task>
1. Открыть файл обработчика Telegram-бота (вероятно, `lib/telegram-bot.ts` или `bot/index.ts`), где находится логика команды `/start`.
2. Добавить вызов базы данных (UPSERT в таблицу `profiles`), используя `SUPABASE_SERVICE_ROLE_KEY` (чтобы пробить RLS, как мы делали в API).
3. Логика UPSERT: вставить `tg_id`, `invites_count = 0`. ВАЖНО: Если профиль уже существует (`ON CONFLICT`), **НЕ перезаписывать** `last_test_date`, чтобы не сбить таймер возврата.
4. Обновить `docs/BRIEF.md`, отметив Phase 7 как полностью завершенную.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения и проверки.
</task>

<rules>
- СТРОГО: Использовать admin client (Service Role Key) для записи в БД, так как анонимные запросы заблокированы RLS.
- Не ломать существующий ответ бота на `/start` (приветственное сообщение и проверка подписки должны работать как раньше).
- Исполнитель: Qwen Code (или Claude Code).
</rules>