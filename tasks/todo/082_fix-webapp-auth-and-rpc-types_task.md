📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Критический сбой E2E-теста (Phase 7). 
Проблема 1: При запуске Mini App через инлайн-кнопку фронтенд выдает экран «Доступ закрыт» (хотя юзер подписан). Запуск через Menu Button работает. Это означает баг в парсинге `initData` или логике проверки `tgWebAppStartParam` в гейткипере Next.js.
Проблема 2: Данные теста всё ещё не сохраняются в БД. RPC `save_test_result` не отрабатывает. Вероятная причина: несовпадение типов (Telegram ID передается как Number, что может вызывать проблемы с BIGINT в Supabase, либо падает сам API route до вызова RPC).
</context>

<task>
1. Исправить проверку подписки (Auth Gate) в Next.js (вероятно в `layout.tsx`, `page.tsx` или `/api/auth/route.ts`). Убедиться, что `initData` корректно извлекается при запуске через инлайн-кнопку бота.
2. Провести аудит `/api/test/submit/route.ts`. Убедиться, что `tg_id` преобразуется в правильный формат (String/BigInt) перед передачей в `supabase.rpc`, так как JS `Number` может конфликтовать с PostgreSQL `BIGINT`.
3. Добавить в `/api/test/submit/route.ts` явный `try/catch` блок, который будет возвращать `error.message` прямо на фронтенд (в `alert` или `console.error` браузера), чтобы Заказчик мог увидеть реальную причину падения без доступа к серверным логам.
4. Написать тестовый скрипт или использовать мок-данные для вызова RPC напрямую из кода Next.js, чтобы на 100% подтвердить работоспособность транзакции.
5. Перенести этот файл из tasks/todo/ в tasks/done/ и предоставить лог с детальным описанием того, какие именно типы или параметры ломали систему.
</task>

<rules>
- СТРОГО: Вернуть текст ошибки RPC на клиент (frontend), чтобы Заказчик мог её увидеть при тесте.
- Не трогать SQL-схему в Supabase.
- Исполнитель: Claude Code (задача требует анализа связки Frontend Auth + Backend API).
</rules>