📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Обнаружен разрыв UX: инлайн-кнопка «Пройти тест» (под сообщениями бота) открывает Mini App как обычную внешнюю ссылку, из-за чего теряется Telegram `initData`, и пользователь получает ошибку «Доступ закрыт». При этом запуск через Menu Button (кнопку интерфейса) работает исправно. Нам нужно изменить тип инлайн-кнопки в Telegram-боте.
</context>

<task>
1. Найти в коде бота (вероятно, в `lib/telegram-bot.ts`, `bot/index.ts` или там, где бот отправляет сообщение «Подписка подтверждена!») формирование клавиатуры (InlineKeyboardMarkup).
2. Заменить параметр кнопки с обычного URL на объект Web App. 
   **Было:** `{ text: "✨ Пройти тест", url: appUrl }`
   **Должно стать:** `{ text: "✨ Пройти тест", web_app: { url: appUrl } }`
3. Убедиться, что это изменение применено везде, где бот предлагает пользователю открыть Mini App (например, в приветственном сообщении /start или после проверки подписки).
4. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- СТРОГО: Не трогать проверку подписки (Auth Gate) во фронтенде (Next.js) — она работает правильно, проблема только в payload'е Telegram-кнопки.
- Сохранить текущий текст на кнопках (✨ Пройти тест).
- Исполнитель: Claude Code или Qwen Code.
</rules>