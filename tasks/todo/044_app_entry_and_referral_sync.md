📋 Прочитай docs/BOOT.md.

<context>
Нужно убрать старые кнопки-ссылки и внедрить вход через Web App с поддержкой реферальных ID.
</context>

<task>
1. В боте: заменить все обычные ссылки на Mini App (кнопки "Открыть") на `web_app` кнопки, указывающие на `process.env.NEXT_PUBLIC_URL`.
2. В коде Mini App (логика инициализации):
   - При запуске проверять `start_param` (реферальный ID). Если он есть, сохранять его в `referred_by` в таблицу `profiles`.
3. Синхронизация БД: при успешном нажатии "Я подписалась" в боте, ПРИНУДИТЕЛЬНО обновлять в Supabase поле `is_subscribed = true` для этого `tg_id`.
4. Убедиться, что Middleware НЕ блокирует запросы к `/api/webhook/telegram`.
5. Перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Проверить, что кнопка в боте теперь открывает "шторку" приложения, а не браузер.
</rules>