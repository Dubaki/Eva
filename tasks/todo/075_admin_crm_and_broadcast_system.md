📋 Прочитай docs/BOOT.md и проверь текущие таблицы в Supabase.

<context>
1. Бот начал присылать результаты, но без фото (нужно исправить Edge Function).
2. Требуется расширить Админ-панель до уровня CRM для отслеживания рефералов и активности.
3. Нужна функция рассылки рекламных сообщений от Автора.
</context>

<task>
**ЧАСТЬ 1: Исправление Edge Function (Фото + Стабильность)**
1. Обновить `supabase/functions/process-bot-notifications/index.ts`:
   - Убедиться, что при INSERT в `test_results` ВСЕГДА вызывается `sendPhoto`.
   - Проверить пути к картинкам (должны быть: `hero.png`, `pleaser.png`, `perfectionist.png`, `stayer.png`, `controller.png`).
   - Добавить логирование ответа Telegram API, если фото не загрузилось.

**ЧАСТЬ 2: Расширенная статистика в AdminPanel**
2. В `app/api/admin/stats/route.ts` добавить отдачу данных:
   - `invites_count` (рефералы).
   - `last_test_date` (из таблицы `test_results`).
   - `next_test_available` (дата `last_test_date` + 60 дней).
   - `created_at` (дата регистрации).
3. На фронтенде Админки (`app/admin/page.tsx`) отобразить это в виде таблицы с возможностью сортировки по количеству рефералов.

**ЧАСТЬ 3: Рассылка (Broadcast)**
4. Создать новый API роут `app/api/admin/broadcast/route.ts` (защита по PIN 2026).
5. Логика рассылки:
   - Получает текст сообщения.
   - Берет все `tg_id` из таблицы `profiles`.
   - Через `Promise.allSettled` или цикл отправляет сообщения через Bot API.
6. В Админ-панели добавить текстовое поле и кнопку "Сделать рассылку всем пользователям".

**ЧАСТЬ 4: Таймер в приложении**
7. В Mini App на кнопке "Пройти тест": если с момента последнего теста прошло < 60 дней, блокировать кнопку и писать: "Опора еще формируется. Повторный тест будет доступен через [X] дней".
</task>

<rules>
- Фото в боте — приоритет №1 (для визуального поиска в чате).
- PIN-код для рассылки обязателен.
</rules>