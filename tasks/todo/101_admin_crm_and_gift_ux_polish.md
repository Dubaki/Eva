<context>
Админка нуждается в функциях CRM для работы с активными пользователями. Необходимо добавить список топ-рефереров с их Username и возможностью отправить им личное сообщение через бота. Также требуется улучшить UX в Mini App при получении подарка: добавить задержку перед закрытием приложения.
</context>

<task>
1. **Функционал CRM в Админке (`app/admin/page.tsx`):**
   - Реализовать таблицу/список пользователей, отсортированный по `invites_count` (от большего к меньшему).
   - Отображать колонки: Username (с активной ссылкой на `t.me/username`), TG ID, Кол-во рефералов.
   - Добавить кнопку "Написать" напротив каждого пользователя. Кнопка должна открывать модальное окно (или инлайн-поле) для ввода сообщения.

2. **API Личных сообщений (`app/api/admin/message/user/route.ts`):**
   - Создать защищенный POST-маршрут, который принимает `target_tg_id` и `text`.
   - Бот должен отправить сообщение указанному пользователю. Реализовать обработку ошибок (например, если пользователь заблокировал бота).

3. **UX Подарка в Mini App (`app/test-result/page.tsx` или аналогичный):**
   - Найти кнопку "Забрать подарок".
   - Модифицировать обработчик клика: 
     1. При клике показать анимацию успеха или сообщение "Переходим к подарку...".
     2. Добавить задержку `setTimeout` на 1.5 - 2 секунды.
     3. После задержки выполнить `window.Telegram.WebApp.close()`.

4. **В конце задачи перенести этот файл task из папки tasks/todo в папку tasks/done.**
</task>

<rules>
- Использовать `supabaseAdmin` для получения списка пользователей.
- Сохранить "дорогой" стиль дизайна из Task 100 для модальных окон и таблиц (мягкие тени, блюр, анимации появления).
- Ссылка на Username должна открываться в новом окне, если `username` не `null`.
</rules>