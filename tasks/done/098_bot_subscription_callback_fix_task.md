<context>
Чтение статуса фронтендом работает идеально. Однако при нажатии инлайн-кнопки "Я подписалась" бот не переключает `is_subscribed` на `TRUE` в базе данных. Проблема кроется внутри обработчика callback_query: либо сбой проверки канала, либо ошибка доступа бота к переменным окружения.
</context>

<task>
1. В файле с логикой бота (`lib/telegram-bot.ts` или `app/api/bot/route.ts` - в зависимости от архитектуры) найти обработчик нажатия кнопки "Я подписалась".
2. Добавить пошаговое логирование в консоль для дебага:
   - Перед проверкой подписки: `console.log("BOT STEP 1: Button clicked by", tgId)`
   - После получения статуса из канала: `console.log("BOT STEP 2: Channel status is", status)`
   - Сразу после запроса к SupabaseAdmin: `console.log("BOT STEP 3: DB Update result:", data, error)`
3. Убедиться, что скрипт бота имеет доступ к `process.env.SUPABASE_SERVICE_ROLE_KEY` (если бот запускается отдельно, проверить импорт `dotenv`).
4. В конце задачи перенести этот файл task из папки tasks/todo в папку tasks/done и обязательно вывести в консоль причину сбоя (Step 1, 2 или 3).
</task>

<rules>
- Не трогать фронтенд-часть (`app/page.tsx` и API роуты), она работает идеально.
- Сохранить текущую структуру меню бота.
- Обеспечить совместимость типов: использовать `Number(tgId)` при записи в БД.
</rules>