📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Несмотря на попытки программного редиректа, Vercel продолжает выдавать 404 DEPLOYMENT_NOT_FOUND при обращении к /app. Нам нужно создать физический роут, который Telegram сможет "увидеть".
</context>

<task>
1. Создать новую директорию `app/app/` (если её нет).
2. Внутри создать файл `app/app/page.tsx`.
3. Содержимое файла должно быть идентично корневому `app/page.tsx` (просто импортировать и экспортировать главный компонент страницы), чтобы по адресу `/app` открывалась та же главная страница.
4. Убедиться, что логика захвата `start_param` из `window.Telegram.WebApp.initDataUnsafe` работает и в этом роуте.
5. Удалить программные `rewrites` из `next.config.js`, которые мы добавляли в таске 121, чтобы не было конфликтов.
6. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Это гарантирует, что Vercel найдет физический путь /app и не выдаст 404.
- Исполнитель: Claude Code.
</rules>