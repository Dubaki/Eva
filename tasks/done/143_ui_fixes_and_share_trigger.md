<context>
Необходимо внести правки в UI после недавнего редизайна (уплотнить верстку, удалить старые тексты), починить логику кнопки "Поделиться" и обновить документацию проекта (BRIEF), чтобы зафиксировать все последние масштабные изменения бизнес-логики.
</context>

<task>
1. **Удаление старого текста (Реферальная страница):**
   - Найти страницу/компонент с реферальной программой.
   - ПОЛНОСТЬЮ удалить блок текста, начинающийся с "Чем больше ты приглашаешь друзей..." и заканчивающийся "...результаты розыгрыша я размещу в канале!".

2. **Починка триггера кнопки "Поделиться":**
   - Сейчас кнопка вызывает только `utils.shareURL()`.
   - Нужно ИЗМЕНИТЬ логику: при клике сначала должен отправляться `fetch` запрос (POST) на внутренний API (например, `/api/user/share-clicked`), передавая данные пользователя (`initData`).
   - На стороне сервера (в API роуте) этот запрос должен вызывать Telegram-бота, чтобы он отправил пользователю 2 обещанных сообщения (инструкцию и текст для пересылки с реф-ссылкой).
   - Только ПОСЛЕ (или параллельно с) успешного `fetch` запроса должен вызываться `utils.shareURL()`.

3. **Компактность UI (Страница результатов / Главная):**
   - На экране, где выводятся кнопки "Забрать приз" и "Связь с Автором" (или при блокировке на 60 дней), нижняя кнопка обрезается на небольших экранах.
   - Сделать верстку более компактной: 
     - Уменьшить `gap` между элементами (текстами и кнопками).
     - Уменьшить размер шрифта описания (про "внутреннюю опору").
     - Уменьшить `padding` (высоту) самих кнопок.
     - Убедиться, что родительский контейнер имеет `overflow-y-auto` и `padding-bottom`, чтобы контент можно было безопасно прокрутить до конца.

4. **Обновление BRIEF (Документации):**
   - Открыть основной файл документации проекта (например, `docs/BRIEF.md` или `README.md`).
   - Актуализировать логику:
     - Описать новый экран выбора после теста ("Открыть за рекомендацию" / "Пока не буду...").
     - Зафиксировать, что сообщение "Бинго!" удалено, награда за 2-х друзей — это только открытие второй опоры (данные из `test_results`).
     - Зафиксировать новые уведомления: сообщения при нажатии "Поделиться", проверка 2-х друзей через 24 часа, автоматическое закрытие приложения после выбора и сообщение про ретест через 2 месяца (60 дней).
     - Указать глобальный UI-стандарт: синие кнопки, белый текст, без эмодзи.

5. Перенести файл в `tasks/done/` после деплоя.
</task>

<rules>
- Не ломать существующий дизайн синих кнопок.
- API для отправки сообщений при шеринге должно быть безопасным и проверять валидность запроса через Telegram initData.
- Исполнитель: Claude Code.
</rules>