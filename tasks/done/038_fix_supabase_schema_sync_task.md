📋 Прочитай docs/BOOT.md перед выполнением задания.

<context>
Логи Vercel показывают ошибку PGRST205 (таблица public.profiles не найдена). База данных была инициализирована вручную. Нужно проверить подключение.
</context>

<task>
1. Проверить все запросы к Supabase в проекте. Убедиться, что используется схема `public` и таблица `profiles`.
2. В файле вебхука (app/api/webhook/telegram/route.ts) убедиться, что при ошибке записи в БД бот НЕ падает, а продолжает работу (отправляет сообщение), логируя ошибку.
3. Добавить в Middleware или API проверку: если база недоступна, выдавать понятную ошибку в консоль, а не 500/401.
4. Перенести этот файл в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Важно: бот должен заговорить, даже если запись в БД временно не удалась.
</rules>