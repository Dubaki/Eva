📋 Прочитай docs/BOOT.md

<context>
Тестирование выявило две проблемы: 
1. Нет возможности вернуться на вопрос назад.
2. Страница результатов не видит завершенный тест и выдает "Результат не найден".
</context>

<task>
1. **Кнопка "Назад" в `app/test/page.tsx`**:
   - Добавить кнопку (например, стрелку или текст "Назад") рядом с номером вопроса.
   - Логика: если `currentQuestion > 0`, уменьшать индекс на 1. 
   - Убедиться, что при возврате назад выбранный ранее ответ сохраняется (выделяется).

2. **Исправление страницы `app/result/page.tsx`**:
   - Проверить, как передаются результаты (через URL query params или через глобальный стейт/localStorage).
   - Если данные в URL, убедиться, что редирект с последнего вопроса на `/result` содержит все необходимые параметры.
   - Убрать заглушку "Пройдите тест...", если расчеты произведены корректно.

3. **Синхронизация**:
   - После правок сделать: `git add .` и `git commit -m "fix: add back button and fix results logic"`.
</task>

<rules>
- Исполнитель: Qwen Code (он лучше справляется со сложной логикой стейтов).
- В этот раз РАЗРЕШАЮ сделать `git push origin main` сразу после коммита, чтобы мы быстрее проверили фикс.
</rules>