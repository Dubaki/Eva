<context>
В приложении заявлена функция 60-дневной блокировки повторного прохождения теста, но она не была реализована в коде. Сейчас пользователи могут проходить тест бесконечно. 
Нужно: 1) сохранять дату завершения теста в `last_test_date`, 2) проверять её при старте теста, 3) блокировать UI, если 60 дней не прошло.

ЗАВИСИМОСТИ: нет
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: 
- `app/api/test/submit/route.ts`
- `app/api/test/progress/route.ts` (или где реализован GET /api/test/progress)
- `app/test/page.tsx`
ТИП: feat / logic
</context>

<task>
1. В `app/api/test/submit/route.ts` найти блок обновления профиля (где обнуляются `current_step` и `question_order`). Добавить туда сохранение текущей даты:
   `last_test_date: new Date().toISOString()`
2. В API получения прогресса (`GET /api/test/progress`) добавить выборку поля `last_test_date` из таблицы `profiles`.
3. Там же добавить логику проверки: если `last_test_date` существует, прибавить к нему 60 дней. Если получившаяся дата больше текущей (Date.now()), вернуть в response специальный флаг: 
   `{ success: true, data: { isCooldown: true, availableAt: <дата разблокировки> } }`
4. В файле `app/test/page.tsx` в функции загрузки прогресса (`useEffect`) обрабатывать флаг `isCooldown`.
5. Добавить новый state `const [cooldownEnd, setCooldownEnd] = useState<string | null>(null)` и устанавливать его, если пришел флаг блокировки.
6. В `app/test/page.tsx` перед рендером вопросов добавить условие: если `cooldownEnd` не null, рендерить экран блокировки с текстом (например: «Повторное прохождение теста будет доступно через 60 дней после предыдущего. Следующая попытка: [Дата]») и скрыть вопросы.
7. ВЕРИФИКАЦИЯ: Пройти тест до конца. Попробовать открыть тест заново. Ожидаемый результат: вместо 1 вопроса появляется заглушка с датой разблокировки.
8. Заполнить COMPLETION LOG в конце этого файла.
9. Перенести этот файл из папки tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Не ломать существующую логику восстановления `current_step`.
- Использовать стандартные функции работы с датами JavaScript.
- Исполнитель: Claude Code (так как требуется аккуратное внедрение нового UI-состояния блокировки) или Qwen Code.
</rules>

---

## COMPLETION LOG
**Статус:** _success_
**Исполнитель:** Gemini
**Изменения:**
- В `app/api/test/submit/route.ts` добавлено сохранение `last_test_date` при завершении теста.
- В `GET /api/test/progress` добавлена проверка 60-дневного кулдауна. Если кулдаун активен, API возвращает `isCooldown: true` и дату разблокировки.
- В `app/test/page.tsx` реализован экран блокировки, который отображается, если пользователь пытается пройти тест до истечения 60 дней.
**Результат верификации:** [x] Успешно