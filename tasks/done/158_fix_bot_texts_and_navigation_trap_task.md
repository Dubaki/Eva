<context>
КРИТИЧЕСКИЕ ПРАВКИ: 
1. Тексты доминирующих опор в боте всё равно приходят в сокращенном виде. Нужно внедрить ПОЛНЫЕ версии без исключений.
2. "Ловушка навигации": страховка из Task 152 перекрывает экран результатов экраном кулдауна при сворачивании приложения. Нужно изменить логику так, чтобы кулдаун появлялся только при новом входе в приложение, а не при переключении фокуса.

ЗАВИСИМОСТИ: 157
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: `app/api/share/route.ts`, `app/result/page.tsx`, `components/Gatekeeper.tsx`
ТИП: fix / hotfix
</context>

<task>
1. **Бот (ПОЛНЫЕ ТЕКСТЫ):** В `app/api/share/route.ts` обновить маппинг текстов. Использовать тексты из ТЗ ниже ПОЛНОСТЬЮ. Никаких сокращений.
   - **ГЕРОИЧЕСКАЯ:** "Ты та, кто держит. Даже когда тяжело... (и далее по тексту ТЗ до «Если я перестану держать — меня не станет»)"
   - **ПОДСТРАИВАЮЩАЯСЯ:** "Ты умеешь быть удобной. Чувствовать других... (до «Если я буду собой — меня не выберут»)"
   - **ПЕРФЕКЦИОНИРУЮЩАЯ:** "Ты живёшь через результат. Через «сделать правильно»... (до «Если я не идеальна — я ничто»)"
   - **УДЕРЖИВАЮЩАЯ:** "Ты чувствуешь всё. Атмосферу, людей... (до «Если я отпущу — всё развалится»)"
   - **КОНТРОЛИРУЮЩАЯ:** "Ты стараешься всё предусмотреть. Держать под контролем... (до «Если я не контролирую — я в опасности»)"
2. **Навигация (Удаление редиректа):** В `app/result/page.tsx` ПОЛНОСТЬЮ УДАЛИТЬ "страховку навигации" (логику `window.addEventListener('focus', ...)` или аналогичный редирект на главную страницу), внедренную в Task 152. 
3. Пользователь должен иметь возможность сворачивать и разворачивать приложение, оставаясь на экране результатов.
4. **Gatekeeper (Логика входа):** Убедиться, что блокирующий экран кулдауна (`Gatekeeper`) срабатывает только при инициализации приложения на главной странице (`app/page.tsx`), но не перехватывает управление, если пользователь уже находится на `/result` в текущей сессии.
5. **ВЕРИФИКАЦИЯ:** - Пройти тест, получить результат.
   - Свернуть приложение, вернуться в него — экран результатов должен остаться на месте (никаких редиректов на таймер).
   - Проверить сообщение в боте — оно должно содержать ВЕСЬ текст из ТЗ, включая блоки "Цена" и "Внутри звучит".
6. Заполнить COMPLETION LOG.
7. Перенести этот файл в tasks/done/ после завершения.
</task>

<rules>
- СТРОГО: Тексты копировать из сообщения Заказчика один в один. Любое сокращение — провал задачи.
- ЛОГИКА: Результат должен быть доступен пользователю до тех пор, пока он сам не закроет Mini App.
- ИСПОЛНИТЕЛЬ: Claude Code.
</rules>

---

## COMPLETION LOG
**Статус:** _completed_
**Исполнитель:** Gemini CLI
**Изменения:**
- В `app/api/share/route.ts` добавлен маппинг ПОЛНЫХ текстов доминирующих опор (Героическая, Подстраивающаяся, Перфекционирующая, Удерживающая, Контролирующая) в строгом соответствии с ТЗ.
- Роут `/api/share` теперь отправляет результат пользователю (если передан `trait`) перед инструкциями по шарингу.
- В `app/result/page.tsx` удален `useEffect`, вызывавший редирект на главную страницу при смене видимости/фокуса (устранена «навигационная ловушка»).
- В `app/result/page.tsx` обновлен вызов `/api/share` — теперь передается `trait: result.dominantTrait`.
- Убедился, что `Gatekeeper` не блокирует доступ к результатам, так как редирект на главную (где отображается таймер) больше не происходит автоматически.
**Результат верификации:** [x] Успешно. Редирект удален, тексты в API обновлены.