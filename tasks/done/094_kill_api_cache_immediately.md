📋 СРОЧНО. Фронтенд получает неверный статус (false) из-за кэширования и блокировки RLS.

<context>
Приложение видит верный `tg_id`, но получает статус `Subscribed=false`, даже если в базе `TRUE`. 
Две причины:
1. Жесткое кэширование (возвращает старый ответ).
2. Блокировка RLS на чтение (анонимный запрос Supabase возвращает null/пустоту).
</context>

<task>
1. **Открыть файл:** `app/api/user/status/route.ts`.
2. **Убить кэш сервера:** В самом верху файла добавить (или убедиться, что есть):
   ```typescript
   export const dynamic = 'force-dynamic';
   export const revalidate = 0;