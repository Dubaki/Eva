📋 Прочитай docs/BOOT.md.

<context>
1. Админ-панель должна быть доступна всем пользователям из списка TESTER_IDS.
2. Системные уведомления (браузерные алерты) всё еще появляются, их нужно полностью заменить на кастомный React-компонент.
</context>

<task>
**ЧАСТЬ 1: Расширение прав на Админ-панель**
1. В коде главного экрана (Dashboard/Results) изменить логику отображения кнопки "👑 Админ-панель":
   - Вместо проверки одного ID `1149371967`, использовать массив `TESTER_IDS = [1149371967, 5930269100, 1419397753]`.
   - Логика: `const CAN_ACCESS_ADMIN = TESTER_IDS.includes(Number(user?.tg_id));`.
2. Убедиться, что сама страница/роут Админ-панели также защищена этой проверкой (чтобы никто посторонний не зашел по прямой ссылке).

**ЧАСТЬ 2: Финальное удаление системных окон (Confirm/Alert)**
3. ПОЛНАЯ ЗАЧИСТКА: Найти в коде и УДАЛИТЬ все вызовы:
   - `window.confirm(...)`
   - `window.alert(...)`
   - `window.Telegram.WebApp.showPopup(...)`
   - `window.Telegram.WebApp.showAlert(...)`
4. Вместо них использовать кастомный компонент модального окна (Custom Modal), созданный в Task 057. Если он еще не реализован как универсальный компонент — вынести его в отдельный UI-элемент, чтобы использовать для любых подтверждений в приложении.
5. Убедиться, что при нажатии на "Да/Нет" в вопросе про результат, всплывает ИМЕННО этот внутренний React-модал, который перекрывает экран черным полупрозрачным фоном.

**ЧАСТЬ 3: Проверка данных в Админке**
6. Убедиться, что в Админ-панели корректно отображаются:
   - Общее число профилей в БД.
   - Список последних 20 регистраций с именами и результатами.
   - Статистика распределения по 5 типам опор.

7. Сделать git push и перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Сравнение ID делать через приведение к Number для надежности.
- Использовать Tailwind CSS для стилизации кастомного модального окна.
</rules>