📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
База данных готова. Теперь нужно реализовать механизм входа. Telegram передает `initData`, которую мы должны валидировать на сервере с помощью `BOT_TOKEN`. Если проверка пройдена — создаем или обновляем запись в таблице `profiles`.
</context>

<task>
1. Создать API Route `app/api/auth/route.ts`.
2. Реализовать валидацию `initData` (HMAC-SHA256) согласно официальной документации Telegram.
3. Логика обработчика:
   - Извлечь данные пользователя (id, username, first_name).
   - Проверить существование профиля в Supabase через `lib/supabase/server.ts`.
   - Если профиля нет — создать (INSERT).
   - Если есть — обновить `updated_at` и `photo_url` (UPSERT).
4. Настроить возвращаемый JSON: успех/ошибка и базовые данные профиля.
5. На фронтенде (в корневом лейауте или провайдере) вызвать этот эндпоинт при загрузке приложения.
6. Обновить `docs/BRIEF.md`: отметить Task 007 как DONE.
7. Перенести этот файл в tasks/done/ после завершения.
</task>

<rules>
- Использовать `BOT_TOKEN` строго из `.env`.
- Никаких паролей — только доверие к валидной подписи Telegram.
- Использовать `lib/supabase/server.ts` (service_role), так как на этапе регистрации у пользователя еще нет JWT для RLS.
- Исполнитель: Claude Code (критическая логика безопасности).
</rules>