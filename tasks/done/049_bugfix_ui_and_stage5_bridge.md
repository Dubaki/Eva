📋 Прочитай docs/BOOT.md и t3.md.

<context>
Необходимо исправить баги UI в боте и Mini App, а также настроить переход к Этапу 5 (Квалификация).
</context>

<task>
**ЧАСТЬ 1: БЭКЕНД БОТА (app/api/webhook/telegram/route.ts)**
1. Починить fallback-сообщения (когда sendPhoto падает). Убедиться, что в `bot.sendMessage` (в блоке catch) ПЕРЕДАЕТСЯ `"parse_mode": "HTML"`. Текст успешной подписки должен выглядеть так же красиво, как и задумывалось, с тегами `<b>` и `<i>`.
2. Убедиться, что к URL фото в `sendPhoto` добавляется `?v=` + `Date.now()`.

**ЧАСТЬ 2: ФРОНТЕНД - СТАРТ И ТЕСТ (Mini App)**
3. Удалить стартовую картинку (`hero.png` или аналогичную) с самого первого экрана перед началом теста. Оставить только заголовок, текст про 2 месяца и кнопку "Пройти тест".
4. Убрать "дыру" (пустое место) на экране вопросов. Использовать `justify-center` или `m-auto` для контейнера вопроса, чтобы карточка с вопросом и кнопками "Да/Нет" располагалась ровно по центру экрана.

**ЧАСТЬ 3: ФРОНТЕНД - РЕЗУЛЬТАТ (Этап 3)**
5. Вывести картинку доминирующей опоры! На экране результата (сразу над текстом опоры) добавить тег `<img src="/result_s.png" />` (имя файла зависит от результата: s, u, p, r или k). Добавить класс `w-full max-h-[40vh] object-contain mb-4`.
6. Изменить текст опроса под результатом: вместо "Ты узнала свою искажённую опору?" написать: "Удивил ли тебя результат?".

**ЧАСТЬ 4: РЕФЕРАЛКА И ПЕРЕХОД К ЭТАПУ 5**
7. Ускорить кнопку шаринга: вместо обычного тега `<a>` использовать API Telegram: 
   `onClick={() => window.Telegram.WebApp.openTelegramLink("https://t.me/share/url?url=...&text=...")}`. Это уберет задержку и позволит вернуться в app.
8. Настроить мост: При нажатии на кнопки "Да" или "Нет" (ответ на "Удивил ли тебя результат?"), пользователь должен переводиться на **Этап 5: Квалификация** (отображение первых вопросов из t3.md: "В какой сфере сильнее чувствуется напряжение?").

9. Сделать git push и перенести таск в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Действовать строго по пунктам. Не ломать существующую логику подсчета баллов.
</rules>