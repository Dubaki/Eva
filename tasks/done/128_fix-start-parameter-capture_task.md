📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Тест показал, что при переходе по реферальной ссылке вида `t.me/sprosievubot?start=5930269100`, колонка `referred_by` в таблице `profiles` остается `NULL`. Бот не сохраняет ID пригласителя при создании/обновлении профиля.
</context>

<task>
1. Найти обработчик команды `/start` в backend-коде Telegram-бота (там, где бот отвечает на первое сообщение пользователя).
2. Настроить парсинг параметра: бот должен извлекать цифры после `/start ` (payload).
3. При INSERT/UPSERT профиля пользователя в БД Supabase, записывать этот извлеченный ID в колонку `referred_by`.
4. ВАЖНО: Проверить типы данных. Если `referred_by` это `int8`, убедиться, что передается число, а не строка, чтобы база не отклоняла запрос молча.
5. Убедиться, что логика начисления `invites_count` (Task 127) ссылается именно на колонку `referred_by`.
6. Перенести файл в tasks/done/ после завершения.
</task>

<rules>
- Изменения касаются только серверной части (бота), которая обрабатывает входящие сообщения Telegram.
- Исполнитель: Claude Code.
</rules>