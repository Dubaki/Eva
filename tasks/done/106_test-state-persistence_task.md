📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Пользователи не всегда проходят тест за одну сессию (пункт 6). Приложение должно запоминать, на каком вопросе или странице остановился пользователь, и возвращать его туда при следующем входе.
</context>

<task>
1. Добавить в таблицу `profiles` в Supabase новую колонку `current_step` (тип integer или text, в зависимости от того, как реализованы шаги в стейте). 
2. Обновить логику прохождения теста: при переходе на следующий вопрос/шаг асинхронно обновлять поле `current_step` в БД.
3. Обновить логику инициализации приложения (`/start` или загрузка App): если у пользователя есть незаконченный тест (`current_step` не null и `last_test_date` старый), маршрутизировать его сразу на сохраненный шаг, подгрузив ответы.
4. Сбрасывать `current_step` в null после успешного полного завершения теста.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Изменения в БД проводить через миграции или MCP аккуратно (не затирая существующие данные).
- Использовать debounce для сохранения шагов, чтобы не спамить запросами БД при быстрых кликах, либо сохранять шаг при маунте нового вопроса.
- Исполнитель: Qwen Code.
</rules>