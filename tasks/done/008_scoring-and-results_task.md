📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Тест из 25 вопросов пройден. Теперь нужно обработать массив ответов, рассчитать баллы по 5 шкалам искажённых опор и показать пользователю его доминирующую опору. Результат должен быть сохранен в таблицу `test_results`.
</context>

<task>
1. В `app/test/page.tsx` реализовать сбор ответов в массив `answers: { questionId: number, score: number }[]`.
2. Создать серверную функцию (Server Action) или API Route `app/api/test/submit/route.ts`:
   - Принимает массив ответов.
   - Валидирует JWT пользователя (использовать `lib/supabase/server.ts`).
   - Рассчитывает средний балл по каждой из 5 шкал (маппинг шкал брать из `lib/questions.ts`).
   - Сохраняет запись в таблицу `test_results`.
3. Создать страницу результата `app/result/page.tsx`:
   - Получает данные из последней записи пользователя в `test_results`.
   - Определяет доминирующую опору (максимальный балл).
   - Визуализирует результат: заголовок, описание опоры и "WOW-эффект" анимация (Framer Motion).
4. Обновить `docs/BRIEF.md`: отметить Task 008 как DONE.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Маппинг шкал должен быть синхронизирован с `lib/questions.ts` (S, U, P, R, K).
- Если баллы по нескольким шкалам равны, выбирать первую по приоритету в методологии.
- UI результата должен соответствовать `.impeccable.md`.
- Исполнитель: Qwen Code (Logic & DB focus).
</rules>