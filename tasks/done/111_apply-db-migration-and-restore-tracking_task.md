📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Миграция БД с колонкой `contact_author_clicked` (089) была создана локально, но не применена к live-базе. Из-за этого CRM падала, и колонку временно убрали из SELECT-запроса. Нужно накатить миграцию и вернуть логику отслеживания.
</context>

<task>
1. Применить локальные миграции к базе данных Supabase (использовать `supabase db push` в терминале или соответствующий MCP-инструмент). Убедиться, что колонка `contact_author_clicked` (boolean) успешно создана в таблице `profiles`.
2. Вернуться в файл `app/api/admin...` (где формируется запрос пользователей для CRM) и вернуть `contact_author_clicked` в `SELECT`.
3. Убедиться, что бейдж/индикатор запроса связи с Автором снова корректно отображается в таблице CRM (как это планировалось в Таске 107).
4. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Не перезаписывать другие данные в БД.
- Исполнитель: Claude Code (так как он работает в терминале и имеет доступ к Supabase CLI/MCP, а также только что редактировал этот API).
</rules>