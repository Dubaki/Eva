📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Использование `AnimatePresence mode="wait"` в Task 032 привело к морганию и дерганью верстки. Контейнер схлопывается в момент ожидания загрузки следующей картинки.
</context>

<task>
1. В `app/test/page.tsx` убрать `mode="wait"` у `AnimatePresence`. Нам нужен кроссфейд (crossfade): старая картинка затухает одновременно с появлением новой.
2. Родительскому контейнеру картинки задать ЖЕСТКУЮ высоту (`style={{ height: '220px' }}` или `h-[220px]`), `relative` и `overflow-hidden`.
3. Самому изображению (внутри `motion.div`) задать `absolute inset-0 object-cover`, чтобы оно не могло раздвинуть или сузить родительский блок ни на пиксель.
4. Добавить пропс `priority={true}` компоненту `<Image />`, чтобы браузер скачивал изображения максимально быстро.
5. Перенести этот файл из tasks/todo/ в tasks/done/.
</task>

<rules>
- Исполнитель: Claude Code.
- Внешний блок не должен менять размер ни при каких условиях анимации.
</rules>