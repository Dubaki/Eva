📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Необходимо глобально отслеживать конверсию переходов в личные сообщения к Автору (t.me/evapatrakhina) и добавить соответствующую кнопку на экран Cooldown.
</context>

<task>
1. На экран Cooldown (где написано сколько дней осталось) добавить кнопку "Связь с Автором" со ссылкой на `https://t.me/evapatrakhina`.
2. Создать глобальную функцию-обертку или onClick-хендлер для ЛЮБОЙ ссылки, ведущей на `t.me/evapatrakhina` внутри Mini App.
3. Логика хендлера: при клике сначала асинхронно отправить запрос к API (Supabase), который установит `contact_author_clicked = true` для текущего `tg_id`, а затем выполнить переход по ссылке (через `Telegram.WebApp.openTelegramLink` или стандартный `window.open`).
4. Проверить, что отметка корректно отображается в CRM Админки.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Переход по ссылке не должен блокироваться, если запрос к БД тупит (использовать Promise.race или небольшую задержку, но приоритет — открыть ссылку).
- Исполнитель: Qwen Code (или Claude).
</rules>