📋 Прочитай docs/BOOT.md.

<context>
КРИТИЧЕСКИЙ БАГ: После прохождения 25 вопросов результат не выводится на экран (показывается "Результат не найден").
Вероятная причина: Race condition (состояние гонки) — редирект или смена экрана происходит до того, как результат успешно записан в Supabase, либо ошибка в самом запросе к БД.
</context>

<task>
**ЧАСТЬ 1: Тяжелое логирование (Tracer Bullets)**
1. В функции, которая срабатывает после ответа на 25-й вопрос (подсчет и сохранение), добавить агрессивные `console.log`:
   `console.log("=== ТЕСТ ЗАВЕРШЕН ===")`
   `console.log("Баллы:", { s, u, p, r, k })`
   `console.log("Доминирующая:", dominant_trait, "Теневая:", shadow_trait)`
2. Обернуть запрос к БД (Supabase / API) в блок `try/catch` и выводить ошибки:
   `catch (error) { console.error("ОШИБКА СОХРАНЕНИЯ В БД:", error); }`

**ЧАСТЬ 2: Фикс асинхронности (Race Condition)**
3. Жестко выстроить цепочку Promise (await):
   - ШАГ 1: Показать экран "Сейчас посчитаю... Это не совсем то, что ты думаешь."
   - ШАГ 2: Выполнить `await` сохранение в Supabase (`dominant_trait` и `shadow_trait`).
   - ШАГ 3: Дождаться ответа от сервера (убедиться, что нет ошибки).
   - ШАГ 4: Искусственная задержка (остаток от 2.5 секунд, если сохранение прошло быстрее).
   - ШАГ 5: ТОЛЬКО ПОСЛЕ УСПЕШНОГО СОХРАНЕНИЯ менять стейт или делать редирект на компонент показа результатов (Этап 3).

**ЧАСТЬ 3: Проверка типов БД**
4. Убедиться, что при сохранении результатов в Supabase отправляются строки (String), желательно в нижнем регистре: `'s'`, `'u'`, `'p'`, `'r'`, `'k'`, так как компонент отображения ожидает именно их.

5. Сделать git push и перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Никогда не переводить пользователя на экран результатов, если сохранение в БД завершилось с ошибкой. В таком случае показывать уведомление об ошибке.
</rules>