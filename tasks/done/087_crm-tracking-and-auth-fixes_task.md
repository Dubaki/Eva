📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Критический апдейт Phase 7. 
Во-первых, нужно исправить баг входа: обычные подписанные пользователи получают «Доступ закрыт» при входе через инлайн-кнопку.
Во-вторых, Заказчик запросил расширенную аналитику (CRM Journey): нам нужно логировать каждый шаг воронки (1 - Нажал старт, 2 - Подписался, 3 - Прошел тест, 4 - Пригласил друга). 
</context>

<task>
1. **DB Migration (Supabase):** Добавить в таблицу `profiles` колонку `subscribed_at` (timestamptz, nullable). Колонка `created_at` (время нажатия старта) там уже должна быть по умолчанию.
2. **Шаг 1 (Захват /start):** В обработчике `/start` бота сделать UPSERT профиля через `supabaseAdmin` (Service Role Key). Если профиль новый — он запишется с текущим `created_at`.
3. **Шаг 4 (Рефералы):** В том же обработчике `/start`: если есть реферальный параметр (например, `/start ref_123`), инкрементировать `invites_count` у пригласившего через `supabaseAdmin`.
4. **Шаг 2 (Подписка):** В обработчике проверки подписки бота (где выдается "Подписка подтверждена"): обновлять колонку `subscribed_at` = `NOW()` для этого `tg_id` через `supabaseAdmin`.
5. **Шаг 3 (Тест):** Эта механика уже работает через RPC `save_test_result` (обновляет `last_test_date` и пишет в `test_results`). Ничего менять не нужно.
6. **Починка Инлайн-входа:** Найти причину, почему инлайн-кнопка (`web_app`) не пускает обычных юзеров. Добавить логирование `initData` в роут валидации (`/api/user/status` или `/api/auth`). Вывести точную ошибку ("Invalid Hash", "No ID") на фронтенд вместо "Доступ закрыт", если валидация падает.
7. Перенести этот файл в tasks/done/ и предоставить лог успешного выполнения.
</task>

<rules>
- СТРОГО: Использовать `SUPABASE_SERVICE_ROLE_KEY` для всех фоновых записей из бота, чтобы обходить RLS.
- Не изменять логику SQL-функции `save_test_result`.
- Исполнитель: Claude Code (задача требует миграции БД, логики бота и дебага фронтенд-авторизации).
</rules>