📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Интеграция RPC (Task 080) завершилась «тихим падением». E2E-тест показал, что после прохождения теста данные НЕ попадают в Supabase (обе таблицы пусты). Кроме того, выявлен баг в системе проверки подписки: бот не пускает подписанного пользователя. Нам нужен срочный дебаг Phase 7.
</context>

<task>
1. Добавить жесткое логирование (`console.log`) в `/api/test/submit/route.ts` (или соответствующий Server Action): логировать входящий payload (особенно `tg_id`), ответ от `supabase.rpc` и сам объект `error`.
2. Проверить клиентский компонент (откуда вызывается API): убедиться, что `tgId` корректно извлекается из `initDataUnsafe` Telegram Web App и не отправляется как `null` или `NaN`.
3. Найти функцию проверки подписки (bot handler) и добавить логирование статуса юзера (`status` из `getChatMember`). Убедиться, что учитываются статусы `member`, `administrator` и `creator`.
4. Исправить найденные разрывы данных. Провести локальный запуск и симуляцию отправки POST-запроса с тестовыми данными.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения и вывести лог с причиной сбоя.
</task>

<rules>
- Не переписывать SQL-функцию, проблема находится на уровне Next.js или бота.
- Не удалять добавленные `console.log` и `console.error` до полного успешного прохождения тестов Заказчиком.
- Исполнитель: Claude Code (или Qwen Code, если Claude недоступен). Задача требует глубокого анализа потока данных.
</rules>