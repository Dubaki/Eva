📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
РЕФЕРАЛЬНЫЙ СЧЕТЧИК НЕ РАБОТАЕТ. Заказчик провел прямое SQL-тестирование базы данных: инкремент и вебхуки работают ИДЕАЛЬНО. Баг находится исключительно в передаче данных от Telegram WebApp (фронтенд) к API во время подтверждения подписки.
</context>

<task>
1. Изучить флоу входа: убедиться, что `start_param` (ID пригласившего) корректно извлекается из `window.Telegram.WebApp.initDataUnsafe` при первом открытии Mini App по реферальной ссылке.
2. Убедиться, что этот `start_param` сохраняется в localStorage или стейте, чтобы не потеряться при переходах по страницам.
3. Найти функцию отправки запроса на бэкенд при ПОДТВЕРЖДЕНИИ ПОДПИСКИ (Subscription Gate).
4. Отредактировать API-запрос: в момент подтверждения подписки фронтенд ОБЯЗАН передать сохраненный ID пригласившего на сервер.
5. Сервер (API) должен вызвать существующую логику инкремента в базе данных для этого переданного ID.
6. Вывести `console.log` на фронтенде: "Deep link param found: [ID]" и "Sending inviter ID to API: [ID]".
7. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- КАТЕГОРИЧЕСКОЕ ТАБУ: НЕ изменять схему базы данных, триггеры или SQL-функции. Они работают идеально. Проблема только в связке Front -> API.
- Исполнитель: Qwen Code.
</rules>