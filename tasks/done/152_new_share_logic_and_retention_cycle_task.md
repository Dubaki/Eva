<context>
Внедрение новой логики Retention (Phase 11.2). Настройка кнопок закрытия и цикличного механизма "Поделиться".

ЗАВИСИМОСТИ: 151
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: `app/result/page.tsx`, `app/api/share/route.ts`, `app/api/test/submit/route.ts`
</context>

<task>
1. **Кнопка «Поделиться» (Экран результатов):**
   - УДАЛИТЬ вызов `utils.shareURL()`.
   - При клике вызывать внутренний API-роут (например, `/api/share`).
   - Логика API:
     а) Проверить `shared_at` в таблице `profiles`.
     б) Если `shared_at` IS NULL:
        - Отправить в бот Сообщение 1: "Скопируй сообщение ниже и отправь его двум друзьям. Когда твои друзья пройдут тест, тебе придёт ответ"
        - Отправить в бот Сообщение 2: "Пройди этот тест и узнай, какой механизм снова и снова приводит тебя к одним и тем же проблемам" + [реферальная ссылка]
        - Записать текущую дату в `shared_at`.
     в) Если `shared_at` НЕ NULL — сообщения НЕ отправлять.
   - ПОСЛЕ вызова API выполнить `window.Telegram.WebApp.close()`.
2. **Страховка навигации:**
   - Если приложение не закрылось, при возврате в фокус оно должно принудительно редиректить на главную страницу (где текст про опору и таймер).
3. **Кнопки закрытия:**
   - Для кнопок "Пока не буду узнавать теневую опору" и "Узнаю через 2 месяца" установить единственный экшен: `window.Telegram.WebApp.close()`.
4. **Механика сброса (Цикл 60 дней):**
   - В роуте `app/api/test/submit/route.ts` добавить код: при каждом успешном сохранении нового теста ОБНУЛЯТЬ колонку `shared_at` (`update({ shared_at: null })`). Это позволит пользователю получить инструкции снова через 60 дней.
5. Заполнить COMPLETION LOG.
</task>

<rules>
- ТЕКСТ: Сообщения в бот вставлять СЛОВО В СЛОВО по ТЗ.
## COMPLETION LOG
**Статус:** _completed_
**Исполнитель:** _Gemini CLI_
**Изменения:**
- Создан API-роут `/api/share` для отправки инструкций по шерингу и реферальной ссылки напрямую в бот.
- Кнопка "Поделиться" на экране результатов теперь вызывает `/api/share` и закрывает приложение.
- Добавлена "страховка навигации" в `app/result/page.tsx`: при возврате фокуса в приложение происходит автоматический редирект на главную страницу.
- Кнопки отказа от дальнейших шагов теперь просто закрывают приложение.
- В `/api/test/submit` добавлено обнуление `shared_at`, что позволяет пользователю снова получить инструкции по шерингу через 60 дней при повторном прохождении теста.