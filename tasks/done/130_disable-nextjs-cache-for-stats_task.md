<context>
Заказчик вручную изменил `invites_count` в базе данных (Supabase), но обновленные данные (5) не отображаются в приложении даже после перезагрузки страницы. Проблема кроется в агрессивном серверном кэшировании Next.js (App Router), которое отдает устаревшую версию страницы. Необходимо отключить кэширование для страниц, отображающих динамическую статистику пользователя.
</context>

<task>
1. Найти страницу или компонент (например, корневой `app/page.tsx` или страницу админки/профиля), где происходит запрос к Supabase для получения профиля и вывода `invites_count`.
2. Добавить в этот файл (если это Server Component) директивы жесткого отключения кэша: `export const dynamic = 'force-dynamic';` и `export const revalidate = 0;`.
3. Если получение данных идет через `fetch`, добавить параметр отключения кэша: `fetch(url, { cache: 'no-store' })`. Убедиться, что клиентские компоненты запрашивают свежие данные при монтировании, а не используют закэшированный payload.
4. Перенести файл `130_disable-nextjs-cache-for-stats_task.md` из папки `tasks/todo/` в папку `tasks/done/` после успешного исправления кода.
</task>

<rules>
- Не изменять логику начисления баллов или логику работы реферальной системы.
- Сохранить текущий UI и стили отображения статистики.
- Обеспечить совместимость с архитектурой App Router в Next.js.
</rules>