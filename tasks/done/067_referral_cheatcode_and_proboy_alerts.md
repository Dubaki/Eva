📋 Прочитай docs/BOOT.md.

<context>
Необходимо протестировать отправку Смешанной (Теневой) опоры в Telegram, не дожидаясь реальных рефералов. 
Также необходимо реализовать уведомление Автора (tg_id 1149371967), когда пользователь нажимает кнопку "Хочу в Пробой", чтобы Автор заранее знал смешанную опору клиента.
</context>

<task>
**ЧАСТЬ 1: Чит-код для проверки рефералки (ВРЕМЕННО)**
1. В логике обработки клика на кнопку `[Получить ссылку]` (Вариант 2 на Шаге 4Б) добавить "чит-код" для немедленной проверки:
   - Сразу после того, как ссылка сгенерирована и показана на экране, сделать API-запрос на бэкенд (создать новый роут `POST /api/test/debug-mixed-trait` или использовать существующий), чтобы принудительно отправить Смешанную опору пользователю в бота.
   - Бэкенд должен определить Смешанную опору пользователя (по логике из Task 066: склейка двух максимальных баллов, например `SP`), взять её текст из словаря и отправить сообщение пользователю через Telegram API `bot.sendMessage`.
   - *Примечание для кодера:* Это временный хак. В будущем это будет срабатывать только по вебхуку/крону, но сейчас нам нужно, чтобы сообщение улетало пользователю прямо в момент клика на кнопку.

**ЧАСТЬ 2: Уведомление Автора при выборе "Пробой"**
2. Обновить роут уведомлений бэкенда (например, `POST /api/notify-author`). Он должен принимать: `userId`, `username`, `firstName` и `mixedTraitName` (Название смешанной опоры, например "S+P — Машина результата").
3. Изменить обработчик кнопки `[Хочу в Пробой]` (Вариант 1 на Шаге 4Б):
   - СНАЧАЛА сделать `fetch` на `/api/notify-author` с передачей данных пользователя и его смешанной опоры.
   - Сервер должен отправить Автору (`tg_id: 1149371967`) сообщение:
     🔥 <b>Заявка в Пробой!</b>
     Пользователь: {firstName} (@{username})
     Смешанная опора: <b>{mixedTraitName}</b>
   - СРАЗУ ПОСЛЕ отправки `fetch` (параллельно, не дожидаясь ответа) выполнить переход в личку:
     `window.Telegram.WebApp.openTelegramLink('https://t.me/evapatrakhina?text=Пробой')`

**ЧАСТЬ 3: Фикс отправки Базовой опоры**
4. Убедиться, что в роуте `/api/test/submit` корректно работает отправка Базовой опоры пользователю в бота. 
   - Проверить, что используется правильный `TELEGRAM_BOT_TOKEN`.
   - Если картинка не отправляется через `sendPhoto`, временно отправлять только текст через `sendMessage`, чтобы убедиться, что проблема не в абсолютных ссылках на изображения. Вывести ошибки отправки в `console.error`.

5. Сделать git push и перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Словарь смешанных опор должен быть доступен как на клиенте (для отправки названия Автору), так и на сервере (для отправки полного текста пользователю).
</rules>