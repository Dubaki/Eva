📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Вторая итерация тестов выявила: картинки всё еще обрезаются (видно 1/3), а реферальная кнопка не выполняет свою функцию (не копирует и не шарит).
</context>

<task>
1. Исправить отображение Hero-фото в `app/test/page.tsx` и `app/result/page.tsx`:
   - Увеличить высоту контейнера или использовать `h-auto max-h-[50vh]`.
   - Заменить `object-cover` на `object-contain`, чтобы картинка была видна ПОЛНОСТЬЮ без обрезки.
   - Убрать агрессивный `mask-image`, оставить только легкий градиент в самом низу (10-15%).
2. Переписать `handleShare` в `app/result/page.tsx`:
   - Сделать логику максимально простой и надежной.
   - Сначала пробовать `navigator.share` (современный стандарт).
   - Если нет — пробовать `window.Telegram.WebApp.openTelegramLink`.
   - Если и это нет (обычный браузер) — ПРИНУДИТЕЛЬНО копировать в буфер обмена и выводить `alert` или `toast` "Ссылка скопирована".
3. Добавить `console.log` для отладки в `handleShare`, чтобы в режиме разработчика было видно, какой метод сработал.
4. Проверить, что переменная `NEXT_PUBLIC_BOT_USERNAME` подтягивается корректно.
</task>

<rules>
- Приоритет: Видимость всей картинки героини.
- Приоритет: Работоспособность кнопки любым способом.
- Исполнитель: Claude Code.
</rules>