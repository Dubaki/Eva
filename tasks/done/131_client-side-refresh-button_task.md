<context>
Серверное кэширование отключено (Task 130 выполнен), однако React-компонент Админки/Профиля по-прежнему удерживает старое состояние (stale state) и показывает 0 вместо 5. Нам нужен механизм принудительного клиентского обновления данных в обход любых кэшей браузера или Next.js.
</context>

<task>
1. Найти React-компонент, который рендерит страницу со статистикой (Админка/Профиль).
2. Если компонент получает данные через клиентский `useEffect` и `fetch` / `supabase.from...`:
   - Добавить кнопку "🔄 Обновить" (или иконку) в UI рядом со статистикой.
   - Написать функцию `fetchData` (или `refreshStats`), которая делает запрос к БД.
   - ПРИВЯЗАТЬ функцию к кнопке (onClick).
   - ВАЖНО: Добавить к запросу механизм "cache-busting", чтобы браузер точно не отдал старый ответ. (Например, для fetch: `cache: 'no-store'`, или добавить `?t=${Date.now()}` к URL запроса).
3. Убедиться, что при успешном запросе обновляется React state (например, `setInvitesCount(newData.invites_count)`), что вызовет перерисовку UI.
4. Перенести файл `131_client-side-refresh-button_task.md` из папки `tasks/todo/` в папку `tasks/done/` после завершения.
</task>

<rules>
- Кнопка должна органично вписываться в текущий дизайн (использовать существующие стили/Tailwind).
- Функция обновления должна менять именно State компонента, чтобы UI отреагировал мгновенно.
- Исполнитель: Claude Code.
</rules>