📋 Прочитай docs/BOOT.md.

<context>
Меняем условие выдачи Смешанной опоры. Теперь рефералу НЕ ОБЯЗАТЕЛЬНО проходить тест. Достаточно просто подписаться на канал автора.
</context>

<task>
1. **Проверка подписки:**
   - В логике бота (обработчик входа по реферальной ссылке) добавить проверку: подписан ли новый пользователь на канал `https://t.me/sprosievu`.
   - Использовать метод Telegram API `getChatMember`.
2. **Запись в БД:**
   - Как только факт подписки подтвержден, инкрементировать поле `invites_count` в таблице `profiles` для пригласившего пользователя (referrer_id).
3. **Edge Function (уже настроена):**
   - Убедиться, что Edge Function `process-bot-notifications` корректно реагирует на изменение `invites_count` с 1 на 2 и отправляет Смешанную опору.
4. **Текст для реферала:**
   - Изменить приветственное сообщение для того, кто перешел по ссылке: "Подпишись на канал автора, чтобы открыть доступ к теневой опоре для подруги, которая тебя пригласила!"
</task>

<rules>
- Условие выдачи: `invites_count === 2`.
- Смешанная опора должна приходить автоматически через Webhook, который мы настроили ранее.
</rules>