Прочитай docs/BOOT.md перед выполнением.

<context>
Мы переносим квалификационную анкету (3 вопроса + выбор продукта) из интерфейса Mini App в Telegram-бота.
Цель: Очистить Mini App от лишних экранов и перевести «дожим» пользователя в чат.
Зависимости: Таблица bot_tasks_queue (создана в БД).
Затрагиваемые файлы: app/page.tsx, app/api/test/submit/route.ts.
Тип: refactor / logic
</context>

<task>

Удаление из Mini App (app/page.tsx):

Полностью удалить стейт funnelStep и связанные с ним компоненты (SURVEY, OFFER, GIFT).

Удалить кнопку «Дальше — больше» и текст «Ты сейчас увидела механизм...».

Страница должна заканчиваться на блоке реферальной ссылки (REFERRAL LINK).

Убедиться, что логика копирования ссылки и шеринга сохранена.

Запуск таймера в API (app/api/test/submit/route.ts):

В роуте POST, сразу после успешного вызова sendPhoto (отправка результата), добавить запись в таблицу bot_tasks_queue.

Поля для записи:

profile_id: из переменной profileId.

tg_id: из переменной tgId.

event_type: 'start_mini_quiz'.

run_at: new Date(Date.now() + 60000) (ровно через 1 минуту).

status: 'pending'.

Верификация:

Пройти тест. После результата должна открыться страница рефералов без лишних кнопок.

Проверить через Supabase Dashboard, что в bot_tasks_queue появилась запись с вашим tg_id и временем выполнения +1 минута.

Заполнить COMPLETION LOG.
</task>

<rules>

Критично: Не удалять handleShare, так как он нужен для реферальной системы.

Async: Запись в очередь должна быть завершена (await) до возврата ответа клиенту.

Исполнитель: Qwen Code.
</rules>

---

## COMPLETION LOG

**Статус:** completed
**Дата завершения:** 2026-04-20
**Тип задачи:** refactor | logic
**Исполнитель:** Gemini CLI

### Сделано
- Удалена квалификационная воронка (survey, offer, gift) из Mini App (`app/result/page.tsx`).
- Удалена кнопка «Дальше — больше» и сопутствующий текст.
- Настроено планирование задачи `start_mini_quiz` в `bot_tasks_queue` через 1 минуту после отправки результата теста в `app/api/test/submit/route.ts`.
- Обновлен `docs/BRIEF.md`.

### Изменённые файлы
- `app/result/page.tsx` — удалена логика воронки и соответствующие UI блоки.
- `app/api/test/submit/route.ts` — добавлена запись в очередь задач бота.
- `docs/BRIEF.md` — обновлен статус дорожной карты.

### Коммит(ы)
- `feat: move qualification funnel to bot and cleanup mini app`

### Верификация
- [x] `npm run build` — passed
- [x] Линтер — passed
- [ ] Ручное тестирование — требует деплоя и проверки в Telegram

### Побочные эффекты / риски
- Пользователи больше не видят опрос и предложение в Mini App сразу после теста. Вся коммуникация перенесена в бота с задержкой в 1 минуту.

### Открытые вопросы
- Нет.
