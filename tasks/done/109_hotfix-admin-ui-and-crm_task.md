📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Критические баги в Админке после редизайна: 1) В разделе CRM пропали все пользователи. Скорее всего, сломан SQL-запрос из-за некорректного добавления колонки `username` (возможно, она NULL у старых юзеров). 2) В статистике так и остался график "Распределение по опорам".
</context>

<task>
1. Починить запрос получения пользователей для таблицы CRM. Убедиться, что используются безопасные JOIN или SELECT (если `username` отсутствует, выводить 'Нет юзернейма' или '-' без падения всего запроса). Вернуть пользователей в таблицу!
2. В компоненте Статистики (Дашборд) ПОЛНОСТЬЮ удалить блок кода, отвечающий за рендер распределения по опорам.
3. Оставить в блоке Статистики только две метрики: "Всего пользователей" и "Прошли Тест".
4. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Использовать `console.error` для отлова ошибки пустого массива пользователей перед исправлением.
- Использовать Impeccable для выравнивания оставшихся двух карточек статистики.
- Исполнитель: Claude Code.
</rules>