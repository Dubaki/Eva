📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Заказчик нашел идеальное место для начисления реферала: момент успешной проверки подписки. Нужно привязать увеличение `invites_count` к событию обновления статуса `is_subscribed` на `TRUE`.
</context>

<task>
1. Добавить колонку `referrer_id` (тип: text или int8) в таблицу `profiles` в Supabase (если её еще нет).
2. Настроить бота: при получении команды `/start <ID>`, если создается новый профиль, записывать этот `<ID>` в колонку `referrer_id`.
3. Найти API-роут или функцию, которая обрабатывает нажатие кнопки "Я подписалась" (Subscription Gate) и ставит `is_subscribed = TRUE`.
4. Внедрить логику: если юзер успешно прошел проверку подписки (статус меняется с False на True) И у него есть `referrer_id`, сделать инкремент: `UPDATE profiles SET invites_count = invites_count + 1 WHERE tg_id = referrer_id`.
5. ВАЖНО: Защита от накрутки. Убедиться, что +1 начисляется только ОДИН РАЗ (например, проверять, что до нажатия кнопки `is_subscribed` был `FALSE`).
6. Перенести файл в tasks/done/ после завершения.
</task>

<rules>
- Это должна быть атомарная операция или транзакция (по возможности), чтобы начисление не сбоило.
- Исполнитель: Claude Code.
</rules>