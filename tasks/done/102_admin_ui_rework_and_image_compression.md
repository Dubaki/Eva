<context>
Заказчик не увидел улучшений премиального дизайна в Админ-панели (Task 100). Необходимо полностью переработать верстку, добавив выраженные эффекты "дорогого" интерфейса. Также требуется внедрить клиентское сжатие изображений перед рассылкой, чтобы файлы весом более 5MB автоматически ужимались до ~500KB для предотвращения перегрузки Telegram API.
</context>

<task>
1. **Переработка Дизайна (`app/admin/page.tsx`):**
   - Заменить скучные таблицы на современные карточки (Cards).
   - Жестко применить Glassmorphism: добавить контейнерам классы `bg-white/70 backdrop-blur-md border border-white/20 shadow-xl rounded-2xl`.
   - Сделать кнопки объемными (например: `bg-gradient-to-r from-blue-500 to-indigo-600 text-white shadow-lg hover:shadow-xl hover:-translate-y-0.5 transition-all`).
   - Убедиться, что `tailwind.config.ts` корректно обрабатывает эти классы.
2. **Клиентское сжатие изображений (`app/admin/page.tsx`):**
   - Найти обработчик загрузки фото для рассылки (`onChange` у input type="file").
   - Написать функцию сжатия на чистом JavaScript (через `HTMLCanvasElement`), которая перехватывает файл.
   - Если файл > 5MB, изменить его размер (max-width/max-height: 1920px) и снизить качество (quality: 0.7 или ниже), пока размер не станет около 500KB.
   - Передавать в `FormData` для отправки на сервер уже сжатый `Blob` вместо оригинального файла.
3. В конце задачи перенести этот файл task из папки tasks/todo в папку tasks/done.
</task>

<rules>
- СТРОГО: Использовать только встроенные возможности браузера (Canvas API) для сжатия фото. Не устанавливать тяжелые сторонние библиотеки вроде `compressorjs`, если можно обойтись нативным кодом.
- Сохранить логику отправки рассылки и CRM без изменений.
- Дизайн должен выглядеть как современное iOS-приложение (много "воздуха", скругления, мягкие градиенты).
</rules>