📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
При переходе между вопросами (особенно после 5-го) происходит сильный Layout Shift — картинка и текст дергаются. Нужно стабилизировать верстку и добавить плавные CSS-переходы.
</context>

<task>
1. В `app/test/page.tsx` задать контейнеру с изображением строгий `aspect-ratio` или фиксированную высоту (например, `h-48` или `h-64`), чтобы он резервировал место до загрузки картинки.
2. Задать блоку с текстом вопроса минимальную высоту (`min-h-[X]`), чтобы варианты ответов не прыгали вверх-вниз при смене длины текста.
3. Добавить плавную анимацию появления/затухания (Fade) при смене `currentQuestionIndex` (использовать `framer-motion` если он есть, либо классы Tailwind `transition-opacity duration-300`).
4. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Исполнитель: Claude Code (скилл Impeccable).
- Никакого "AI-слопа" и прыгающих кнопок. Переход должен ощущаться как нативное мобильное приложение.
</rules>