📋 Прочитай docs/BOOT.md.

<context>
Воронка работает, но требуются финальные правки текста, UI реферального блока и логика закрытия приложения после принятия оффера.
</context>

<task>
**ЧАСТЬ 1: Обновление условий реферальной программы**
1. В блоке реферальной ссылки (Шаг 3/4Б) заменить текст:
   Было: "Когда 2 человека перейдут по ней и подпишутся, я открою тебе второй слой"
   Стало: "Когда 2 человека перейдут по ней и пройдут тест, я открою тебе второй слой - твою теневую опору"

**ЧАСТЬ 2: UI Реферальной ссылки**
2. Переделать отображение персональной ссылки:
   - Вместо большой отдельной кнопки "Скопировать ссылку" сделать компактный блок.
   - Ссылка должна быть в инпуте или текстовом поле, а СЛЕВА или СПРАВА от неё — маленькая кнопка (иконка) копирования.
   - При нажатии на иконку должен срабатывать `navigator.clipboard.writeText(link)` и появляться краткое уведомление (например, тултип или смена иконки) "Скопировано".

**ЧАСТЬ 3: Автоматическое закрытие приложения (Exit Logic)**
3. Реализовать закрытие Telegram WebApp (`window.Telegram.WebApp.close()`) в следующих точках выхода:
   - **Кнопка "Жёсткий быстрый"** (Пробой): Сначала открывается ссылка в личку к Автору, затем вызывается `close()`.
   - **Кнопка "Мягкий постепенный"** (Пирамида): Сначала открывается ссылка в личку к Автору, затем вызывается `close()`.
   - **Кнопка "Забрать подарок"**: После открытия ссылки в личку к Автору и отправки сообщения ботом, приложение должно закрыться.
   - *Важно:* Вызывать `close()` нужно через небольшую задержку (100-200мс) после `openTelegramLink`, чтобы браузер успел обработать переход в чат.

4. Сделать git push и перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Использовать иконку из библиотеки (например, Lucide React или простую SVG) для кнопки копирования.
- Тексты использовать СТРОГО из ТЗ.
</rules>