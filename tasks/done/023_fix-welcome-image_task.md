📋 Прочитай docs/BOOT.md

<context>
Кнопка на главной работает, навигация исправна. Осталось добавить приветственное изображение на страницу `app/page.tsx`. Картинка `hero.png` уже лежит в папке `public/` в нижнем регистре.
</context>

<task>
1. Открыть файл `app/page.tsx`.
2. Найти место для изображения (выше текста приветствия).
3. Добавить компонент Next.js `<Image>` (не забыть импорт `import Image from 'next/image'`).
4. Установить атрибуты:
   - `src="/hero.png"` (строго нижний регистр, со слешем в начале).
   - `alt="EVA Welcome"`
   - `width={350}`
   - `height={350}`
   - `priority={true}` (чтобы загрузилась мгновенно).
   - `className="mx-auto mb-6 rounded-2xl"` (центрирование и отступы).
5. Проверить, что код компилируется без ошибок.
6. Перенести этот файл в tasks/done/.
7. ⚠️ Сообщить Заказчику, что код готов к `git push`.
</task>

<rules>
- Исполнитель: Claude Code.
- НЕ делать git push самостоятельно.
</rules>