📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Проект готов к деплою на Vercel. Файл `.gitignore` проверен, секреты защищены. Необходимо инициализировать репозиторий и отправить весь код в удаленный репозиторий Заказчика на GitHub.
</context>

<task>
1. Используя MCP-инструмент для выполнения команд в терминале (shell/bash), выполни следующую цепочку команд в корне проекта (`C:\eva`):
   - `git init`
   - `git add .`
   - `git commit -m "feat: EVA v1.0 Production Ready"`
   - `git branch -M main`
   - `git remote add origin https://github.com/Dubaki/Eva.git` (игнорировать ошибку, если origin уже существует)
   - `git push -u origin main`
2. Внимательно следить за выводом терминала. Если Git запросит интерактивную авторизацию и процесс зависнет, прервать выполнение и сообщить Заказчику, что требуется ручной ввод.
3. Убедиться, что папки `node_modules` и файлы `.env.local` не попали в коммит (проверить вывод `git add` или `git status`).
4. Перенести этот файл в tasks/done/ после успешного push.
</task>

<rules>
- Исполнитель: Qwen Code.
- Строго соблюдать последовательность команд.
- Не пытаться коммитить секреты, если .gitignore настроен неверно (сделать предварительную проверку `git status`).
</rules>