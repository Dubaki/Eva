📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Приложение запущено у Заказчика, выявлены критические огрехи в UX: спойлер названия опоры, плохая видимость фото и нерабочая кнопка шеринга вне TMA.
</context>

<task>
1. Исправить "спойлер" в `app/result/page.tsx`:
   - Название вторичной опоры должно быть строго скрыто (заменено на "???") до тех пор, пока `refCount >= 2`.
2. Поправить отображение Hero-фото:
   - Изменить `mask-image`: сделать градиент мягче, чтобы 80% фото сверху было четким.
   - Добавить `object-fit: cover` и `object-position: center 20%`, чтобы лица не обрезались.
3. Оживить реферальную кнопку:
   - Внедрить проверку: если `window.Telegram.WebApp` недоступен, использовать `navigator.clipboard.writeText` для копирования ссылки.
   - Добавить визуальный фидбек при клике (текст кнопки меняется на "Скопировано!" на 2 сек).
4. Проверить отступы на странице результатов (i-audit), чтобы блоки не наезжали друг на друга.
5. Перенести этот файл в tasks/done/ после завершения.
</task>

<rules>
- Сохранить стиль Impeccable.
- Обеспечить работу кнопки как внутри Telegram, так и в обычном браузере (debug-mode).
- Исполнитель: Claude Code.
</rules>