<context>
Система успешно сохраняет UUID пригласителя в колонку `referrer_id` (тип uuid) при переходе по ссылке. Однако при нажатии кнопки "Я подписалась" (callback `check_sub`), счетчик `invites_count` у пригласителя не увеличивается. Нужно добавить логику инкремента в момент подтверждения подписки.
</context>

<task>
1. Найти обработчик callback-кнопки проверки подписки (например, `check_sub_...` в `app/api/webhook/telegram/route.ts` или аналогичном файле).
2. Найти место, где происходит обновление профиля пользователя: `is_subscribed` меняется с `false` на `true`.
3. Добавить логику начисления балла: 
   - Сразу после успешного обновления `is_subscribed = true`, запросить `referrer_id` текущего пользователя.
   - Если `referrer_id` существует, сделать инкремент: увеличить `invites_count` на 1 в таблице `profiles` ГДЕ `id = referrer_id`.
   - Добавить логирование: `console.log('Reward +1 given to referrer:', referrer_id)`.
4. ВАЖНО: Убедиться, что начисление происходит только если юзер *впервые* подтвердил подписку (проверка `wasAlreadySubscribed === false`), чтобы избежать накрутки повторными нажатиями.
5. Перенести файл в `tasks/done/` после выполнения.
</task>

<rules>
- Использовать колонку `referrer_id` (тип uuid) для поиска пригласившего.
- Инкремент должен быть безопасным (в идеале через RPC `increment_invites`, либо считывание текущего значения и +1).
- Исполнитель: Claude Code.
</rules>