📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Кнопка "Пригласить друзей" в `app/result/page.tsx` (Task 031) не нажимается (тихо падает). Нужно добавить перехват ошибок и fallback-вариант, чтобы функционал не блокировался.
</context>

<task>
1. В обработчике клика кнопки "Пригласить" обернуть вызов `openTelegramLink` в `try...catch`.
2. В блоке `catch` добавить `alert('Ошибка: ' + err.message)`, чтобы Заказчик видел, почему кнопка не работает.
3. Добавить проверку: если `window.Telegram?.WebApp?.openTelegramLink` недоступен, сделать Fallback — скопировать ссылку в буфер обмена (`navigator.clipboard.writeText`) и показать Toast/Alert "Ссылка скопирована!".
4. Убедиться, что сама ссылка (URL) формируется корректно, даже если `NEXT_PUBLIC_APP_NAME` не успел загрузиться из env (добавить жесткий fallback на `'sprosievubot'`).
5. Перенести этот файл из tasks/todo/ в tasks/done/.
</task>

<rules>
- Исполнитель: Claude Code.
- Кнопка должна давать обратную связь при клике В ЛЮБОМ случае (либо открывает ТГ, либо копирует, либо выдает ошибку).
</rules>