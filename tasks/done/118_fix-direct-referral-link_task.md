📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Заказчик локализовал критический баг реферальной системы: приложение генерирует ссылку на бота (`?start=ref_ID`), а не прямую ссылку на Mini App (`?startapp=ID`). Из-за этого WebApp открывается «слепым» и не получает ID пригласившего. Известно точное короткое имя приложения: `app`.
</context>

<task>
1. Найти компонент или функцию, где генерируется реферальная ссылка (кнопка "Поделиться" / "Пригласить").
2. Жёстко задать правильный формат ссылки: `https://t.me/sprosievubot/app?startapp=[TG_ID]`. (Где `[TG_ID]` — это ID текущего пользователя-пригласителя).
3. Убедиться, что в главном layout или на стартовой странице считывается `window.Telegram.WebApp.initDataUnsafe.start_param`. Если он есть, его нужно сохранить в `localStorage` (например, как `inviter_id`).
4. Найти функцию отправки запроса на подтверждение подписки на бэкенд. Проследить, чтобы этот `inviter_id` из `localStorage` передавался в API (и API выполняло инкремент в базе данных).
5. Использовать `Telegram.WebApp.openTelegramLink` для расшаривания новой ссылки.
6. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- КАТЕГОРИЧЕСКОЕ ТАБУ: Использовать старый формат `?start=`.
- Логика базы данных работает идеально (проверено вручную), НЕ трогать SQL, RPC и триггеры.
- Исполнитель: Claude Code.
</rules>