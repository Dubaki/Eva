📋 Прочитай docs/BOOT.md.

<context>
1. Бот не отправляет сообщения с результатами (Telegram API интеграция не сработала).
2. Необходимо обновить текстовые блоки на фронтенде (Этап 4-6).
3. Необходимо реализовать отправку Смешанной опоры (Теневой) в Telegram-бота, когда пользователь набирает 2 рефералов.
</context>

<task>
**ЧАСТЬ 1: Жесткая отправка в Telegram (Бэкенд)**
1. Создать/обновить утилиту для отправки сообщений в Telegram (например, `lib/telegram.ts`):
   - Использовать нативный `fetch` к `https://api.telegram.org/bot${process.env.TELEGRAM_BOT_TOKEN}/sendPhoto` и `/sendMessage`.
2. В роуте `api/test/submit` (после сохранения результатов) вызвать эту утилиту:
   - Отправить фото (`sendPhoto`) доминирующей опоры (сформировать абсолютный URL к картинке, например `https://твой-домен.vercel.app/hero.png`).
   - Отправить текст (описание опоры) через `caption` или отдельным `sendMessage`.
   - Если отправка упадет, залогировать ошибку, но не ломать выполнение фронтенда (не возвращать 500 ошибку клиенту).

**ЧАСТЬ 2: UI Правки (Фронтенд)**
3. На Шаге 4Б (Блок "Я обычно открываю этот слой..."):
   - Добавить текст перед вариантами: "есть два варианта, как получить доступ:"
   - Добавить заголовки к блокам:
     - "Вариант 1:" (перед Пробоем)
     - "Вариант 2:" (перед Открыть через участие)
4. В Блоке реферальной ссылки:
   - Сразу после вывода сгенерированной ссылки добавить кнопку "Закрыть приложение".
   - Обработчик кнопки: `onClick={() => window.Telegram?.WebApp?.close()}`
5. На Шаге 4А (Оффер):
   - Исправить форматирование текста. Использовать `<br/>` или CSS, чтобы каждое предложение начиналось с новой строки:
     "Есть 2 способа работы с искаженной опорой:<br/><br/>✓ Жёсткий, но быстрый — это группа \"Пробой\"<br/>✓ Мягкий и постепенный — это \"Пирамида Потенциала\" или персональная работа<br/><br/>Какой способ тебе ближе?"

**ЧАСТЬ 3: Движок Смешанных Опор (Бэкенд - Обработчик Рефералов)**
6. Создать словарь (Map) текстов смешанных опор. Важно: ключи должны быть отсортированы по алфавиту или по приоритету (чтобы S+U и U+S выдавали один результат `SU`).
   - `SU` (S+U): "Тихий тащитель\nТы тащишь.\nИ делаешь это тихо...\n⚡ Внутри звучит:\n«Я всё делаю правильно… почему меня не выбирают?»"
   - `SP` (S+P): "Машина результата\nТы работаешь на максимум...\n⚡ Внутри звучит:\n«Я должна быть сверхчеловеком»"
   - `RS` (S+R): "Опора для всех\nТы держишь не только себя...\n⚡ Внутри звучит:\n«Без меня всё развалится»"
   - `KS` (S+K): "Железная система\nТы сильная и всё контролируешь...\n⚡ Внутри звучит:\n«Я должна удержать всё любой ценой»"
   - `PU` (U+P): "Идеальная для всех\nТы стараешься быть идеальной...\n⚡ Внутри звучит:\n«Если я не идеальна — меня не выберут»"
   - `RU` (U+R): "Спасатель\nТы живёшь через помощь другим...\n⚡ Внутри звучит:\n«Я нужна, только если я полезна»"
   - `KU` (U+K): "Тревожный угодник\nТы стараешься угадать, как правильно...\n⚡ Внутри звучит:\n«Если я ошибусь — меня отвергнут»"
   - `PR` (P+R): "Социальный идеал\nТы пытаешься быть идеальной...\n⚡ Внутри звучит:\n«Я должна соответствовать всем»"
   - `KP` (P+K): "Тревожный достигатор\nТы живёшь через контроль и идеальность...\n⚡ Внутри звучит:\n«Ошибка — это катастрофа»"
   - `KR` (R+K): "Сканер угроз\nТы постоянно на чеку...\n⚡ Внутри звучит:\n«Я должна всё предусмотреть, иначе будет плохо»"
   *(ВНИМАНИЕ КОДЕР: используй полные тексты из ТЗ пользователя)*

7. В логике начисления рефералов (webhook или проверка подписки):
   - Если `referrals_count` пользователя становится ровно 2:
   - Сформировать ключ из `dominant_trait` и `shadow_trait` (например, отсортировать их по алфавиту и склеить: `K` и `S` -> `KS`).
   - Получить текст смешанной опоры из словаря.
   - Отправить пользователю в Telegram сообщение (пока без картинки, только текст): "🎉 Твой второй уровень открыт! Пришло время узнать твою теневую опору:\n\n" + текст смешанной опоры.

8. Сделать git push и перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Для смешанных опор использовать полные тексты, предоставленные пользователем.
- Убедиться, что отправка в Telegram не блокирует основной UI-поток.
</rules>