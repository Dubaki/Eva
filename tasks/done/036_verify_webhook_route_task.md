📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Telegram webhook настроен на `URL/api/webhook/telegram`, но бот не реагирует на /start. Нужно жестко проверить наличие правильного роута и добавить максимальное логирование для дебага на Vercel.
</context>

<task>
1. Убедиться, что файл вебхука находится строго по пути `app/api/webhook/telegram/route.ts`. Если он называется иначе или лежит в другой папке (например, `app/api/webhook/route.ts`) — переименовать/переместить его для точного совпадения с URL.
2. В самом начале обработчика `POST` добавить агрессивное логирование: `console.log("=== WEBHOOK RECEIVED ===", ...)` чтобы выводить весь входящий JSON от Telegram.
3. Обернуть логику проверки подписки и отправки приветственного фото в `try...catch`.
4. В блоке `catch` добавить вывод ошибки: `console.error("WEBHOOK CRASH:", error)`.
5. Убедиться, что бот всегда возвращает `new Response('OK', { status: 200 })` в конце `POST` запроса, даже если произошла ошибка, чтобы Telegram не спамил повторными запросами.
6. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Исполнитель: Qwen Code.
- Не ломать логику проверки подписки (Этап 1), только добавить логирование и обработку ошибок.
</rules>