📋 Прочитай docs/BOOT.md перед выполнением.

<context>
1. MarkdownV2 ломает текст из-за экранирования. Нужно срочно перевести всего бота на `parse_mode: "HTML"`.
2. Картинки всё еще не отправляются (возможно, используется fallback на sendMessage).
3. Верстка стартового экрана Mini App (frontend) сломана: картинка слишком большая (появляется скролл), нет отступов между блоками "25 вопросов" и кнопкой "Пройти тест".
</context>

<task>
**ЧАСТЬ 1: БЭКЕНД БОТА (app/api/webhook/telegram/route.ts)**
1. Полностью удалить использование `MarkdownV2`. Везде жестко установить `"parse_mode": "HTML"`.
2. В обработчике `/start`:
   - Использовать ТОЛЬКО метод `sendPhoto`.
   - `photo`: `"https://eva-9udm.vercel.app/start.png"`
   - `caption` в формате HTML (в точности этот текст):
     <b>ТВОЯ ВНУТРЕННЯЯ ОПОРА</b> 💎

     Привет! Я создала этого бота, чтобы помочь тебе найти ответы, которые уже есть внутри тебя.

     ✦ <b>Почему это важно?</b>
     В мире хаоса единственное, на что можно опереться — это <i>ты сама</i>.

     ▹ <b>Твой первый шаг:</b>
     Подпишись на мой канал. Там я даю эксклюзивные практики и знания, которых нет в открытом доступе.

3. В обработчике успешной подписки `check_subscription`:
   - Использовать ТОЛЬКО метод `sendPhoto`.
   - `photo`: `"https://eva-9udm.vercel.app/start1.png"`
   - `caption` в формате HTML:
     🎉 <b>Подписка подтверждена!</b>

     У каждого человека есть внутренняя «опора» — набор убеждений, которые помогают жить и развиваться.
     Но иногда эти установки начинают искажаться, и мы теряем связь с собой.

     👇 <i>Нажми кнопку ниже, чтобы начать тест.</i>

**ЧАСТЬ 2: ФРОНТЕНД MINI APP (app/page.tsx или компонент главного экрана)**
4. Исправить проблему со скроллом и размером картинки:
   - Общий контейнер экрана должен быть `h-screen`, `flex`, `flex-col`, `overflow-hidden`.
   - Картинка должна иметь классы для адаптивности, например: `w-full max-h-[35vh] object-cover rounded-b-3xl shrink-0`.
5. Исправить "воздух" между блоками:
   - Добавить явный отступ (`margin-top` или `gap`) между контейнером с фактами ("25 вопросов", "5 минут") и основной кнопкой "✨ Пройти тест". Например, обернуть кнопку в `div` с классом `mt-6 mb-8` или увеличить `gap` в родительском `flex`-контейнере.
6. Убедиться, что контент внутри приложения центрирован и не выходит за границы экрана телефона.

7. Перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Никаких слешей (\) для экранирования в HTML!
- Если `sendPhoto` выдает ошибку, выводить в `console.error` полный объект ошибки, но НЕ переключаться на `sendMessage` с сырым текстом, мы должны починить именно отправку фото.
</rules>