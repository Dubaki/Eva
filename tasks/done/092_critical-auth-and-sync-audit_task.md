📋 Прочитай docs/BOOT.md. Это критическая остановка проекта.

<context>
ПОЛНЫЙ ОТКАЗ СИСТЕМЫ АВТОРИЗАЦИИ:
1. Бот подтверждает подписку, но НЕ меняет FALSE на TRUE в базе.
2. Даже ручное изменение на TRUE в базе НЕ открывает доступ в Mini App.
Система полностью заблокирована.
</context>

<task>
1. **Аудит Роута Статуса (`/api/user/status/route.ts`):**
   - Проверить, какой параметр он принимает. Если `tg_id`, то убедиться, что запрос к Supabase выглядит так: `.eq('tg_id', tgId)`.
   - УБЕДИТЬСЯ, что роут не кэширует ответ (добавить `export const dynamic = 'force-dynamic'`).
   - Проверить, что возвращается именно колонка `is_subscribed`.

2. **Аудит Брони во фронтенде (`app/page.tsx`):**
   - Вывести в `console.log` ответ от API. Почему при `is_subscribed: true` в базе, фронтенд видит `false`? 
   - Проверить соответствие типов: `tg_id` из Telegram (число) vs `tg_id` в запросе к API.

3. **Аудит Бота (`lib/telegram-bot.ts`):**
   - Почему `supabaseAdmin.update` не срабатывает? 
   - ПРОВЕРИТЬ: передается ли `process.env.SUPABASE_SERVICE_ROLE_KEY` в среду бота. Если бот запущен в отдельном процессе, он может НЕ ВИДЕТЬ переменные из `.env.local`.
   - ЗАМЕНИТЬ `update` на жесткий вызов через `rpc`, если обычный `update` игнорируется.

4. **Логирование:** Добавить в каждый шаг `console.log` с префиксом `!!! CRITICAL !!!`, чтобы мы видели путь данных от кнопки до ячейки в БД.
</task>

<rules>
- СТРОГО: Исправить несоответствие имен переменных (tg_id vs id vs userId).
- Исполнитель: Claude Code (требуется глубокий анализ связей).
</rules>