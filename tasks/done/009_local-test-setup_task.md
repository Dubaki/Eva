📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Заказчик хочет лично протестировать приложение локально. Сейчас Auth Guard блокирует доступ без данных от Telegram. Нужно внедрить механизм обхода для разработки и дать четкую инструкцию по запуску.
</context>

<task>
1. В `components/providers/AuthProvider.tsx` добавить проверку: если `process.env.NODE_ENV === 'development'` и в URL есть параметр `?debug=true`, имитировать успешную авторизацию с тестовым профилем.
2. В `app/api/auth/route.ts` добавить обработку тестового режима, чтобы сервер не выдавал 403 при отсутствии подписи в dev-среде.
3. Проверить, что все внешние ресурсы (картинки персонажей) имеют корректные fallback-заглушки, если файлы еще не загружены в `/public`.
4. Написать в лог Итоговую Инструкцию для Заказчика:
   - Команды для терминала.
   - Ссылка для открытия в браузере.
   - Тестовые данные для .env.
5. Перенести этот файл в tasks/done/ после завершения.
</task>

<rules>
- НЕ отключать валидацию в `production` режиме.
- Сохранить Impeccable UI.
- Исполнитель: Claude Code.
</rules>