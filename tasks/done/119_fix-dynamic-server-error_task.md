📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Деплой на Vercel упал с ошибкой DYNAMIC_SERVER_USAGE в роуте `/api/test/results`. Next.js не может скомпилировать страницу статически, так как используются `request.headers`. Нужно явно пометить роут как динамический.
</context>

<task>
1. Открыть файл `app/api/test/results/route.ts` (или аналогичный, указанный в ошибке).
2. В самый верх файла (после импортов) добавить строку:
   `export const dynamic = 'force-dynamic';`
3. Убедиться, что в коде роута корректно обрабатываются заголовки и нет синтаксических ошибок.
4. Проверить аналогичные API-роуты (`/api/admin/stats` и т.д.) на наличие такой же настройки, если они используют headers или cookies.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Не удалять логику проверки заголовков.
- Использовать стандарт Next.js 14 для динамических роутов.
- Исполнитель: Claude Code.
</rules>