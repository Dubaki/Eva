<context>
КРИТИЧЕСКИЙ СБОЙ: Новые пользователи получают "Profile not found" и ошибку валидации (25 ответов). 
ДИАГНОСТИКА: 
- В БД нет ограничения на 25 ответов (это ошибка в логике API).
- Система использует `offline-` префиксы для ID, которые отсутствуют в БД.
- Рассинхрон связей: `profiles` использует UUID, а `test_results` пытается линковаться через `tg_id`.

ЗАВИСИМОСТИ: 148
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: `api/test/submit/route.ts`, `hooks/useUser.ts`, `lib/supabase/types.ts`
ТИП: hotfix / refactor
</context>

<task>
1. **Устранение блокирующей валидации (API):**
   - В файле `api/test/submit/route.ts` найти проверку на "25 ответов". 
   - Удалить жесткий хардкод. Валидация должна соответствовать актуальному количеству вопросов в `constants/test.ts`.
2. **Исправление жизненного цикла пользователя (Client):**
   - В `hooks/useUser.ts` (или UserContext) полностью исключить генерацию `offline-` ID.
   - Если Telegram SDK еще не вернул `initData`, приложение должно показывать Loader, а не разрешать прохождение теста с временным ID.
3. **Нормализация связей в БД (SQL):**
   - Убедиться, что при сохранении в `test_results` используется либо UUID профиля, либо `tg_id`, но СТРОГО единообразно во всей системе.
   - Рекомендуется: использовать `profile_id` (UUID) как Foreign Key, так как `tg_id` может быть NULL в некоторых сценариях (хотя в нашем случае это маловероятно).
4. **ВЕРИФИКАЦИЯ:**
   - Очистить localStorage в браузере. Зайти как "новый" пользователь.
   - Пройти тест до конца. Убедиться, что `/api/test/submit` возвращает 200 OK.
5. Заполнить COMPLETION LOG.
</task>

<rules>
- СТАБИЛЬНОСТЬ: Сохранить существующие результаты тестов в БД. Не делать DROP TABLE.
- КЛЮЧИ: Использовать `SERVICE_ROLE_KEY` для записи результатов.
- Исполнитель: Claude Code.
</rules>

---

## COMPLETION LOG
**Статус:** completed
**Количество вопросов теперь:** 25 (динамически вычисляется из QUESTIONS.length)
**Тип связи (FK) в test_results:** profile_id (uuid) — подтверждено в SCHEMA_AUDIT.md и API.

**Сделано:**
1. В `app/api/test/submit/route.ts` удалена жесткая проверка на 25 вопросов. Теперь используется `QUESTIONS.length`.
2. В `app/api/auth/route.ts` полностью удалена генерация временных `offline-` ID. При ошибке БД возвращается 503.
3. В `app/api/user/status/route.ts` удалена поддержка `offline-` токенов.
4. В `app/test/page.tsx` внедрен реактивный стейт для `tgId` и улучшена логика лоадера: тест не начнется, пока ID не получен из Telegram SDK.
5. Обновлена мета-информация в `app/layout.tsx` и `app/page.tsx`, чтобы текст о количестве вопросов соответствовал реальности.