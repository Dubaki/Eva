<context>
Код инкремента в `check_sub` написан, но в логах Vercel после обновления подписки наступает тишина. Подозрение на то, что `currentUser` возвращается пустым или логика начисления не срабатывает из-за отсутствия обработки ошибок.
</context>

<task>
1. Переписать блок начисления реферала в `app/api/webhook/telegram/route.ts` (ветка `!wasAlreadySubscribed`).
2. Внедрить ЖЕСТКОЕ логирование каждого чиха:
   - `console.log('!!! STEP 4 !!! Current User tg_id:', userId)`
   - `console.log('!!! STEP 4.1 !!! Found referrer_id:', currentUser?.referrer_id)`
3. Вместо сложного `select`, если это возможно, использовать переменную профиля, которая БЫЛА получена при обновлении на шаге 3 (чтобы не делать лишний запрос в БД).
4. Если `referrer_id` найден, выполнить начисление через `update` и ОБЯЗАТЕЛЬНО добавить логирование результата:
   - `if (error) console.error('!!! STEP 5 ERROR !!!', error)`
   - `else console.log('!!! STEP 5 SUCCESS !!! New count set for:', referrer_id)`
5. Убедиться, что UUID пригласителя (referrer_id) используется для поиска через `.eq('id', ...)` (по первичному ключу), а не через `tg_id`.
6. Перенести файл в `tasks/done/` и задеплоить.
</task>

<rules>
- Не удалять логику уведомления о "второй опоре" (при достижении 2 рефералов).
- Использовать только явные логи с пометкой `!!!`, чтобы мы видели их в Vercel.
</rules>