📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Текущие Direct Mini App Links (/join) ведут сразу в приложение, минуя Subscription Gate в боте. Это блокирует новых юзеров. Нужно вернуться к классическим реферальным ссылкам через бота.
</context>

<task>
1. Изменить формат генерации реферальной ссылки в приложении на: `https://t.me/sprosievubot?start=[TG_ID]`. (Внимание: используется `?start=`, а не `startapp`).
2. В коде Telegram Bot (backend): добавить обработку команды `/start` с параметром. 
   - Если пришел `/start 5930269100`, бот должен временно сохранить эту связь (referrer_id) в базе данных для этого нового user_id.
3. В логике Subscription Gate (внутри приложения): если `start_param` в WebApp пуст, приложение должно проверить в БД, не был ли этот юзер приглашен кем-то ранее через бота (по таблице рефералов).
4. Убедиться, что при нажатии кнопки "Запустить" в боте после подписки, реферал окончательно засчитывается.
5. Перенести этот файл из tasks/todo/ в tasks/done/.
</task>

<rules>
- Ссылка должна вести в ЧАТ, а не открывать приложение.
- Параметр `start` в боте и `startapp` в приложении — это разные механизмы, нужно поддержать оба для надежности.
- Исполнитель: Claude Code.
</rules>