📋 Прочитай docs/BOOT.md и t3.md.

<context>
1. Системные алерты (window.confirm и showPopup) портят дизайн доменным именем. Нужно заменить их на кастомный React Modal.
2. Необходимо реализовать интерфейс Администратора для Автора (tg_id: 1149371967).
</context>

<task>
**ЧАСТЬ 1: Кастомное окно подтверждения (Удаление алерта)**
1. В компоненте Этапа 3 полностью удалить `window.confirm` и `window.Telegram.WebApp.showPopup`.
2. Создать собственный UI-компонент (Modal / Dialog):
   - При нажатии "Да" или "Нет" показывать поверх контента красивую плашку (с затемнением фона): 
     **Заголовок:** "Уверена?"
     **Текст:** "Ты не сможешь изменить свой ответ."
     **Кнопки:** [Подтвердить] и [Отмена].
3. Только после клика на [Подтвердить] внутри этого кастомного модального окна — продолжать логику (показывать скрытый текст и кнопку Далее). Никаких системных окон браузера!

**ЧАСТЬ 2: Кнопка Администратора**
4. На главном экране (Дашборд результатов) добавить проверку:
   `const IS_AUTHOR = user.tg_id === 1149371967;`
5. Если `IS_AUTHOR` равен `true`, выводить секретную кнопку: "👑 Админ-панель". Клик по ней должен переводить на новый экран/компонент Администратора.

**ЧАСТЬ 3: Экран Админ-панели (Аналитика)**
6. Создать компонент/страницу `AdminPanel`.
7. Реализовать запросы к Supabase (к таблице `profiles`) для сбора статистики:
   - **Общее количество пользователей:** (count всех записей).
   - **Прошли тест:** (count тех, у кого есть результат `dominant_trait`).
   - **Статистика по опорам:** Сколько человек получили S, U, P, R, K.
   - **Список последних лидов:** Вывести таблицей или списком карточек последние 20 пользователей (Имя, @username, Результат теста).
8. Добавить кнопку "🔙 Назад в приложение".

9. Сделать git push и перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Дизайн Админ-панели должен быть аккуратным, но без излишеств — это чисто утилитарный инструмент.
- Запрещено использовать `window.alert` или `window.confirm` где-либо в проекте.
</rules>