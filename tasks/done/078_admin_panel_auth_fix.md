📋 СТАТУС: БАГ АВТОРИЗАЦИИ. Владелец не может войти в Админ-панель.
При вводе корректного PIN-кода (2026) вход не происходит.

<context>
Страница блокировки (/admin) не пускает пользователя дальше ввода пароля. Не сохраняется сессия авторизации, либо падает API-роут проверки.
</context>

<task>
1. **Проверка API & Env:**
   - Убедиться, что роут проверки PIN-кода работает корректно. 
   - Если PIN сверяется с переменной окружения (например, `ADMIN_PIN`), убедиться, что есть fallback на `2026` для тестовой среды, или что переменная добавлена.

2. **Проверка State / Cookies:**
   - Проверить логику сохранения успешного входа (localStorage, sessionStorage или HTTP-only cookie).
   - Приложение не должно "выбрасывать" администратора при перезагрузке страницы.

3. **Логика редиректа:**
   - После успешного ввода PIN-кода должен происходить автоматический переход к таблице пользователей и форме рассылки (компоненты CRM).
   - Обработать ошибку: если введен неверный PIN, должно появляться красное уведомление "Неверный PIN-код", а не просто молчаливый сброс.
</task>

<report_requirements>
ОБЯЗАТЕЛЬНО: Прислать GIF или видео экрана.
Сценарий на видео: Ты вводишь PIN 2026 -> нажимаешь "Войти" -> успешно попадаешь внутрь панели администратора -> обновляешь страницу (F5) -> тебя НЕ выкидывает обратно на экран ввода пароля.
Без этого визуального подтверждения задача не принимается.
</report_requirements>