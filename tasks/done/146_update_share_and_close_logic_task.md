<!-- BOOT: Перед выполнением прочитай docs/BOOT.md -->

<context>
Необходимо обновить логику взаимодействия с Telegram SDK в Mini App. В частности, требуется корректная инициация окна "Share" (Поделиться) через `utils.shareURL()` и автоматическое закрытие приложения через `utils.close()` после выполнения ключевых действий, чтобы улучшить UX и Retention. Ранее были замечены случаи, когда приложение не закрывалось или окно Share не вызывалось после API-запроса.
</context>

<task>
1. Найти компоненты, отвечающие за реферальную систему и результаты теста (`components/referral/` и `components/results/`).
2. Обновить вызов `utils.shareURL()`: убедиться, что он происходит строго после успешного завершения API-запроса на бэкенд (пре-хит Share-триггера).
3. Добавить вызов `utils.close()` в обработчики кнопок, где требуется завершение сессии (например, после успешного получения подарка или подтверждения действия).
4. Проверить импорты из `@twa-dev/sdk` и типизацию `WebApp`.
5. Убедиться, что логика работает плавно на мобильных устройствах.
</task>

<rules>
- Не ломать существующую логику подсчета рефералов.
- Сохранить стилистику UI (анимации Framer Motion).
- Не удалять существующие `console.log` дебага, если они критичны для тестирования в боте.
</rules>

---

## COMPLETION LOG

**Статус:** completed
**Дата завершения:** 2026-04-16
**Тип задачи:** ui | sdk
**Исполнитель:** Gemini CLI

### Сделано
- Переведена логика шаринга на нативный метод `WebApp.utils.shareURL()`.
- Упорядочена последовательность: сначала API-запрос для CRM, затем вызов окна Share.
- Внедрено автоматическое закрытие приложения через `WebApp.close()` при переходе в чат к автору и при получении подарка.
- Удалены устаревшие проверки `window.Telegram` в пользу `@twa-dev/sdk`.

### Изменённые файлы
- `app/result/page.tsx` — основная логика шаринга и закрытия.
- `lib/author-contact.ts` — встроено закрытие Mini App при клике на контакт автора.

### Коммит(ы)
- `feat: update share and close logic using Telegram SDK (task 146)`

### Верификация
- [x] Импорты SDK проверены.
- [x] Логика `setTimeout` для закрытия после перехода в чат сохранена.
- [x] Пре-хит API для CRM работает перед вызовом нативного окна.

