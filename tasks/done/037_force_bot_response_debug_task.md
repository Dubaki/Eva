📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Бот молчит на /start. Домен https://eva-9udm.vercel.app/ подтвержден как рабочий. В логах Vercel отсутствуют входящие запросы к вебхуку. Нужно обеспечить доступность эндпоинта и отказоустойчивость ответа.
</context>

<task>
1. Проверить существование файла `app/api/webhook/telegram/route.ts`. Если его нет, создать.
2. В обработчике `POST` добавить: `console.log(">>> incoming request to webhook")`.
3. Реализовать Этап 1 из t3.md:
   - Отправка фото (через `sendPhoto`) с кнопками "Подписаться" и "Я подписалась".
   - Текст: "Привет! Перед тем как начать, подпишись на канал. Там я даю информацию, которую не даю больше нигде."
4. Обернуть `sendPhoto` в try/catch. Если оно падает (например, неверный URL картинки), ПРИНУДИТЕЛЬНО отправить тот же текст через `sendMessage`.
5. Исправить 401 ошибку в `/api/qualification/submit`: разрешить доступ без токена (Guest Mode), как в `/api/test/submit`.
6. Убедиться, что Middleware НЕ блокирует роут `/api/webhook/telegram` (добавить его в исключения, если нужно).
7. Перенести этот файл в tasks/done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Использовать TELEGRAM_CHANNEL_ID (-1001792041386) и URL из env.
- Возвращать 200 OK в любом случае.
</rules>