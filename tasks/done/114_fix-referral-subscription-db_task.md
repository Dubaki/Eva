📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Критический баг архитектуры БД: сейчас рефералы вообще не сохраняются, либо счетчик срабатывает просто от клика. Заказчик требует: счетчик рефералов в Админке (колонка invites_count) должен увеличиваться ТОЛЬКО тогда, когда приглашенный человек реально ПОДПИСАЛСЯ на канал и зашел в приложение.
</context>

<task>
1. Найти эндпоинт API или Edge Function, которая обрабатывает подтверждение подписки пользователя (там, где проверяется `is_subscribed` или сохраняется новый профиль после прохождения гейта).
2. Изменить логику инкремента: `invites_count` у пригласившего (`inviter_tg_id`) должен увеличиваться на +1 СТРОГО в момент успешного подтверждения подписки рефералом.
3. Убедиться, что глубокая ссылка (deep link) вида `/start ref_12345` корректно парсится, и `inviter_tg_id` сохраняется во временном стейте или сессии до момента проверки подписки.
4. Проверить SQL/Supabase RPC-запрос: он должен делать `UPDATE profiles SET invites_count = invites_count + 1 WHERE tg_id = [INVITER_ID]`.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Не трогать колонку `contact_author_clicked`.
- Исполнитель: Qwen Code.
</rules>