📋 Прочитай docs/BOOT.md.

<context>
1. Текст отображается с сырыми символами (\\*\\*). Нужно жестко передавать `parse_mode: "MarkdownV2"` в запросах к Telegram API.
2. При успешной подписке нужно отправлять фото `start1.png` с красивым текстом, а не просто текст.
</context>

<task>
1. В `app/api/webhook/telegram/route.ts` убедиться, что во всех вызовах `sendPhoto` и `sendMessage` в body запроса присутствует:
   `"parse_mode": "MarkdownV2"`

2. Переписать обработчик УСПЕШНОЙ подписки (`check_subscription`):
   - Использовать метод `sendPhoto`.
   - URL фото: `https://eva-9udm.vercel.app/start1.png`.
   - В `caption` передать этот текст (не забудь правильно экранировать спецсимволы для MarkdownV2!):
   
   🎉 **Подписка подтверждена\!**
   
   У каждого человека есть внутренняя «опора» — набор убеждений, которые помогают жить и развиваться\.
   Но иногда эти установки начинают искажаться, и мы теряем связь с собой\.
   
   👇 *Нажми кнопку ниже, чтобы начать тест\.*

3. Убедиться, что под этим фото выводится InlineKeyboardButton с параметром `web_app`, ведущим на `process.env.NEXT_PUBLIC_URL`. Текст кнопки: `✨ Пройти тест`

4. Проверить `caption` для команды `/start` — убедиться, что `parse_mode: "MarkdownV2"` применяется и там, чтобы убрать видимые слеши.

5. Перенести в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Следить за правильным JSON форматом для Telegram API (parse_mode должно быть на верхнем уровне объекта, рядом с chat_id).
</rules>