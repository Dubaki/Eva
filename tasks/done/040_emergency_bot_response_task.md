📋 Прочитай docs/BOOT.md перед выполнением.

<context>
Бот молчит из-за ошибок в схеме Supabase (отсутствующие колонки). Нужно изменить приоритет: сначала ответ пользователю, затем работа с БД.
</context>

<task>
1. В `app/api/webhook/telegram/route.ts` переписать логику обработки `/start`:
   - ПЕРВЫМ ДЕЛОМ вызвать `sendMessage` (или `sendPhoto`) с приветствием из ТЗ.
   - Использовать `try...catch` ВНУТРИ асинхронной функции записи в БД, чтобы ошибка `upsert` в таблицу `profiles` не прерывала выполнение основного скрипта.
2. В блоке `catch` для базы данных оставить только `console.error("DB SILENT ERROR:", error)`, не выбрасывая ошибку дальше.
3. Полностью удалить (закомментировать) Middleware, если он проверяет что-то, кроме путей `/admin` (сделать API эндпоинты бота полностью открытыми).
4. В ответ на POST запрос всегда возвращать `return NextResponse.json({ ok: true })` в конце, независимо от успеха записи в БД.
5. Перенести этот файл в done/.
</task>

<rules>
- Исполнитель: Qwen Code.
- Цель: пользователь должен получить сообщение в Telegram ПРИ ЛЮБОМ состоянии базы данных.
</rules>