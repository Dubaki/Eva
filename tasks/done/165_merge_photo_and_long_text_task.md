📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
ТЕКУЩАЯ СИТУАЦИЯ: Бот присылает два сообщения с результатом теста: одно короткое с фото (отрабатывает старая логика Edge Function) и одно длинное без фото (новая логика прямой отправки из route.ts).
ЦЕЛЬ: Объединить их в ОДНО сообщение: Фото + Полный текст в подписи (caption). Полностью исключить дублирование от Edge Function. Логика подсчета (S > U > P > R > K) проверена и работает верно, трогать её не нужно.
</context>

<task>

Открыть файл app/api/test/submit/route.ts.

Найти блок отправки в Telegram (там, где вызываются sendPhoto и sendMessage).

Изменить логику отправки так, чтобы полный текст (fullText) передавался как caption внутри функции sendPhoto.

Реализовать фолбэк: если sendPhoto завершится ошибкой (например, из-за недоступности URL картинки на localhost), бот должен автоматически отправить этот же текст через sendMessage, чтобы пользователь в любом случае получил результат.

КРИТИЧНО: Найти и полностью УДАЛИТЬ вызов функции triggerBotNotification({ event: &#39;dominant_trait_set&#39;, ... }) в этом файле. Именно она виновата в отправке старого короткого сообщения.

Верифицировать результат (локально или на dev): пройти тест, убедиться, что приходит ровно ОДНО сообщение с картинкой и полным текстом под ней.

Заполнить COMPLETION LOG.

Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.

Ожидаемый код отправки:

TypeScript
    try {
      // 1. Пытаемся отправить ФОТО + ТЕКСТ в одном сообщении
      const sent = await sendPhoto({
        chatId: tgId,
        photo: photoUrl,
        caption: fullText,
        parseMode: 'HTML'
      });

      // 2. Если фото не отправилось, отправляем только ТЕКСТ
      if (!sent) {
        await sendMessage({
          chatId: tgId,
          text: fullText,
          parseMode: 'HTML'
        });
      }
      console.log('[API] Result sent successfully as a single message');
    } catch (err) {
      console.error('[API] Critical failure sending results:', err);
    }

    // ВНИМАНИЕ: Любой вызов triggerBotNotification(..., 'dominant_trait_set', ...) ниже должен быть удален!
</task>

<rules>

СТРОГО: В итоге должно уходить только одно сообщение (фото + текст).

СТРОГО: Обязательно удалить вызов triggerBotNotification для dominant_trait_set.

Не изменять логику calculateScores.

Исполнитель: Qwen Code.
</rules>

## COMPLETION LOG
**Статус:** _completed_
**Исполнитель:** Gemini CLI
**Изменения:**
- В `app/api/test/submit/route.ts` логика отправки в Telegram объединена: теперь фото и полный текст (caption) отправляются одним вызовом `sendPhoto`.
- Добавлен автоматический фолбэк на `sendMessage` (только текст), если отправка фото не удалась (например, при тестировании на localhost или если текст превышает 1024 символа).
- Удален неиспользуемый импорт `triggerBotNotification`, а его вызовы для результатов теста были удалены ранее для исключения дублирования.
**Результат верификации:** [x] Успешно. Приложение отправляет одно сообщение с фото и полным текстом.