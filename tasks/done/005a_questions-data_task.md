📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Для работы теста EVA необходим структурированный массив вопросов. Всего 25 вопросов, разделенных по 5 шкалам искажённых опор. Нужно создать типизированный конфиг, который будет использоваться на фронтенде для рендеринга и на бэкенде для валидации ответов.
</context>

<task>
1. Создать файл `lib/questions.ts`.
2. Описать интерфейс `Question`:
   - id: number
   - text: string
   - scale: 'performance' | 'perfection' | 'pleasing' | 'control' | 'hyper-vigilance' (названия шкал согласно методологии 5 опор)
3. Экспортировать константный массив `QUESTIONS`, содержащий 25 объектов (заполни рыбой-текстом на русском языке, соблюдая равномерное распределение по шкалам — по 5 на каждую).
4. Экспортировать вспомогательную функцию `getQuestionById(id: number)`.
5. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- Использовать строго TypeScript.
- Никакого внешнего состояния или API в этом файле, только статические данные.
- Исполнитель: Qwen Code.
</rules>