📋 Прочитай docs/BOOT.md перед выполнением.

<context>
Бот работает. Нужно внедрить новое визуальное оформление приветствия и исправить проверку подписки (chat not found). 
Файл фото: https://eva-9udm.vercel.app/start.png
</context>

<task>
1. В `app/api/webhook/telegram/route.ts` полностью переписать ответ на `/start`:
   - Использовать метод `sendPhoto`.
   - В `caption` вставить следующий текст (использовать parse_mode: 'MarkdownV2'):

   * **ТВОЯ ВНУТРЕННЯЯ ОПОРА** 💎
   * * Привет! Я создала этого бота, чтобы помочь тебе найти ответы, которые уже есть внутри тебя.
   * * ✦ **Почему это важно?**
   * В мире хаоса единственное, на что можно опереться — это *ты сама*.
   * * ▹ **Твой первый шаг:**
   * Подпишись на мой канал. Там я даю эксклюзивные практики и знания, которых нет в открытом доступе.

2. Исправить логику кнопки "check_subscription" (обработчик callback_query):
   - Использовать `TELEGRAM_CHANNEL_ID` из окружения.
   - Считать успешной подписку при статусах: 'member', 'administrator', 'creator'.
   - В случае успеха: отправить сообщение "Подписка подтверждена! 🎉" и кнопку запуска Web App.
   - В случае ошибки (400 chat not found): логировать ID канала и выводить пользователю "Ошибка: проверьте права бота в канале".

3. Убедиться, что Inline-кнопки под фото оформлены красиво:
   - [ 📢 Подписаться ] (ссылка на канал)
   - [ ✅ Я подписалась ] (callback_data: check_subscription)

4. Перенести файл в done/.
</task>

<rules>
- Использовать экранирование спецсимволов для MarkdownV2 (точки, дефисы и т.д.).
- Бот должен присылать картинку и текст ОДНИМ сообщением.
</rules>