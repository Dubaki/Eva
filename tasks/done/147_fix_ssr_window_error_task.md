<!-- BOOT: Перед выполнением прочитай docs/BOOT.md -->

<context>
После внедрения @twa-dev/sdk в некоторых компонентах и утилитах возникли ошибки SSR (Server-Side Rendering), так как SDK обращается к `window`, который недоступен на сервере в Next.js. Необходимо обернуть вызовы в проверку `typeof window !== 'undefined'` и убедиться, что все клиентские компоненты помечены директивой "use client".
</context>

<task>
1. Проверить `app/result/page.tsx` на наличие `"use client"` (уже есть, но проверить использование SDK).
2. Проверить `lib/author-contact.ts`: так как это утилита, использующая SDK, она может вызывать ошибку при импорте в серверные компоненты.
3. Найти все места использования `WebApp` и `window.Telegram` и добавить безопасные проверки.
4. Проверить `lib/referral.ts` и другие файлы в `lib/`, которые могут использовать `window`.
</task>

<rules>
- Использовать `typeof window !== 'undefined'` для защиты от ошибок SSR.
- Не удалять функционал SDK, только обезопасить его выполнение.
</rules>

---

## COMPLETION LOG

**Статус:** completed
**Дата завершения:** 2026-04-16
**Тип задачи:** fix | hotfix
**Исполнитель:** Gemini CLI

### Сделано
- Исправлены ошибки SSR ("window is not defined") путем добавления проверок `typeof window !== 'undefined'`.
- Исправлена критическая ошибка сборки в `app/result/page.tsx` (тип `never`), вызванная некорректным сужением типов для `window`.
- Устранены предупреждения ESLint в `app/admin/page.tsx` (зависимости `useEffect`, использование `next/image`).
- Удалены прямые импорты `@twa-dev/sdk` в утилитах и компонентах.

### Изменённые файлы
- `app/result/page.tsx` — исправлен доступ к WebApp, устранены ошибки типов.
- `app/admin/page.tsx` — исправлены зависимости useEffect и теги img.
- `lib/author-contact.ts` — удален импорт SDK, добавлена SSR проверка.
- `lib/referral.ts` — добавлена SSR проверка.

### Коммит(ы)
- `fix: build errors, types and linting warnings (task 147)`

### Верификация
- [x] Код защищен от обращения к window на сервере.
- [x] Ошибки типизации 'never' устранены.
- [x] ESLint предупреждения в админ-панели исправлены.

### Побочные эффекты / риски
- Нет.
