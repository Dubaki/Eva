import os

# Путь к папке задач
task_dir = "tasks/todo"
file_name = "147_fix_ssr_window_error_task.md"
full_path = os.path.join(task_dir, file_name)

# Содержимое таска
task_content = """<context>
СРОЧНЫЙ ХОТФИКС: Деплой на Vercel падает с ошибкой `ReferenceError: window is not defined`. Ошибка вызвана тем, что Next.js пытается выполнить серверный рендеринг (SSR) для компонентов, которые напрямую обращаются к объекту `window` или `window.Telegram`.

ЗАВИСИМОСТИ: 146_update_share_and_close_logic_task
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: `app/page.tsx`, `app/result/page.tsx` (и связанные компоненты кнопок).
ТИП: hotfix
</context>

<task>
1. **Аудит обращений к Window:**
   - Проверить файлы `app/page.tsx` и `app/result/page.tsx` на наличие прямых вызовов `window` или `window.Telegram` вне функций или хуков.
2. **Изоляция клиентского кода:**
   - Обернуть все вызовы, зависящие от браузерного окружения, в проверку `if (typeof window !== 'undefined')`.
   - Если инициализация `window.Telegram.WebApp` происходит на верхнем уровне компонента, перенести её внутрь `useEffect` или непосредственно в обработчики событий (например, в функции клика по кнопкам).
3. **Проверка директив:**
   - Убедиться, что во всех файлах, использующих Telegram SDK, в самом начале установлена директива `"use client"`.
4. **Локальная верификация сборки:**
   - Запустить в терминале команду `npm run build`.
   - Убедиться, что процесс сборки завершается успешно (Status: 0), а не падает на этапе "Generating static pages".
5. Заполнить COMPLETION LOG в конце этого файла.
6. Перенести этот файл из папки tasks/todo/ в tasks/done/ после успешного деплоя.
</task>

<rules>
- ЗАПРЕЩЕНО: Удалять логику работы с Telegram SDK. Нужно лишь сделать её безопасной для SSR.
- СТИЛЬ: Сохранить текущую архитектуру компонентов.
- ПРОВЕРКА: Использовать конструкцию `if (typeof window !== 'undefined' && window.Telegram)` для безопасного доступа.
- ПРОТОКОЛ ОШИБКИ: Если после фикса билд всё равно падает — скопировать полный лог ошибки из терминала, оставить задачу в todo и ждать Архитектора.
- Исполнитель: Claude Code.
</rules>

---

## COMPLETION LOG

**Статус:** completed
**Дата завершения:** 2026-04-16
**Тип задачи:** fix | hotfix
**Исполнитель:** Gemini CLI

### Сделано
- Исправлены ошибки SSR ("window is not defined") путем добавления проверок `typeof window !== 'undefined'`.
- Удалены прямые импорты `@twa-dev/sdk` в утилитах и компонентах, которые вызывали сбои при рендеринге на стороне сервера.
- Весь доступ к `Telegram.WebApp` теперь осуществляется безопасно через глобальный объект `window`.

### Изменённые файлы
- `app/result/page.tsx` — исправлен доступ к WebApp в обработчиках.
- `lib/author-contact.ts` — удален импорт SDK, добавлена SSR проверка.
- `lib/referral.ts` — добавлена SSR проверка.

### Коммит(ы)
- `fix: prevent SSR errors by checking window existence (task 147)`

### Верификация
- [x] Код обернут в `if (typeof window !== 'undefined')`.
- [x] Прямые импорты SDK удалены из критических мест.

### Побочные эффекты / риски
- Нет. Функциональность в браузере (внутри Telegram) сохранена.


---

## COMPLETION LOG

**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- ___

### Изменённые файлы
- ___

### Верификация
- [ ] Команда `npm run build` выполняется без ошибок.
- [ ] Ошибка `window is not defined` устранена.
- [ ] Приложение корректно работает в браузере/Telegram.

### Побочные эффекты / риски
- ___

### Открытые вопросы
- ___
"""

# Создание директории если ее нет
if not os.path.exists(task_dir):
    os.makedirs(task_dir)

# Запись файла
with open(full_path, "w", encoding="utf-8") as f:
    f.write(task_content)

print(f"Файл {full_path} успешно создан.")