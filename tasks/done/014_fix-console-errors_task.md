📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Во время локального тестирования в консоли выявлены три ошибки: Hydration mismatch из-за стилей Telegram, пустая реферальная ссылка в debug-режиме и 400 Bad Request при отправке квалификации (якобы не заполнены поля).
</context>

<task>
1. Исправить ошибку гидратации:
   - В `app/layout.tsx` добавить `suppressHydrationWarning` к тегу `<html>` (или `<body>`, где нужно).
2. Починить квалификацию `app/result/page.tsx` и `app/api/qualification/submit/route.ts`:
   - Проверить, как собирается стейт 3-х вопросов перед отправкой `POST` запроса. Убедиться, что ключи в JSON совпадают с тем, что ожидает API.
   - Добавить логирование отправляемого `payload` (console.log) перед `fetch`, чтобы видеть, что уходит на сервер.
3. Починить реферальную ссылку в `debug` режиме (`app/result/page.tsx`):
   - Убедиться, что моковый профиль в `debug=true` имеет `tgId` (или `id`), чтобы функция `getReferralLink` не возвращала `undefined`/пустоту.
   - Добавить fallback: если `refLink` всё равно пуст, использовать базовую ссылку на бота.
4. Перенести этот файл в tasks/done/ после завершения.
</task>

<rules>
- Использовать строго типизацию TypeScript.
- Исполнитель: Qwen Code.
</rules>