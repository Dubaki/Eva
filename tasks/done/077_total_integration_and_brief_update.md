📋 СТАТУС: КРИТИЧЕСКАЯ ОШИБКА. Система не работает в связке. 
Данные из Mini App не попадают в БД. Блокировка не активна. Документация устарела.

<context>
При ручном тестировании владельцем выявлено:
1. Записи в `test_results` автоматически не создаются после прохождения теста в Mini App.
2. Cooldown (блокировка 60 дней) на фронтенде не работает, даже если в БД изменить дату вручную.
3. Уведомления в бот не приходят (следствие п.1).
</context>

<task>
1. **Frontend-to-DB Link (Fix):**
   - Проверить функцию `submitTest` (или аналог) в Mini App. Почему она не делает INSERT в `test_results`?
   - Проверить RLS Policies (Row Level Security) в Supabase. Разрешен ли INSERT для пользователя?
   - ОБЯЗАТЕЛЬНО: Реализовать строгую транзакцию (или последовательность). Сначала делать `UPSERT` в таблицу `profiles`, и только затем `INSERT` в `test_results`.

2. **Frontend Lock (Fix):**
   - При запуске Mini App сделать запрос к актуальной записи пользователя в БД.
   - Если `last_test_date` (или дата последней записи в `test_results`) < 60 дней, кнопка "Пройти тест" ДОЛЖНА быть заблокирована на уровне UI.

3. **Обновление документации (Update Brief):**
   - Открыть главный файл документации (например, `docs/BOOT.md` или `Brief`).
   - Описать актуальную схему БД: наличие связи Foreign Key между `test_results` и `profiles`.
   - Зафиксировать наличие и назначение колонки `reminded_at` в таблице `profiles`.
   - Описать архитектуру таймера (Cooldown 60 дней) и как работают вебхуки для отправки результатов в Telegram.

4. **Отчетность (ОБЯЗАТЕЛЬНО ДЛЯ ПРИЕМКИ):**
   - Без предоставления визуальных доказательств задача считается невыполненной.
</task>

<report_requirements>
1. Скриншот консоли браузера (вкладка Network) в момент нажатия кнопки "Завершить тест", где виден успешный ответ от базы (HTTP 200/201).
2. Видео экрана: прохождение теста как НОВЫЙ пользователь -> появление новой строки в Supabase (`profiles` и `test_results`) -> моментальное получение сообщения в Telegram-бот.
3. Ссылка на коммит или текст изменений, внесенных в главный Brief проекта.
</report_requirements>